# Course preview v18_STRICT_PEDAGOGY

This file is a methodist preview only. Import course_import.json as the source of truth.

Modules: 5
Lessons: 169
Steps: 1960

# Модуль 1. Python Core, терминал, Git

## 001. Первый код

Estimated hours: 2.3
Topics: Python Core

### 001. Смысл (theory)

Программа — это набор команд, которые компьютер выполняет по порядку. В Python команда обычно занимает одну строку кода. Когда ты запускаешь файл, Python читает первую строку, выполняет её, затем переходит к следующей. Поэтому порядок строк важен: если поменять строки местами, результат тоже может измениться.

Самая маленькая полезная команда для старта — `print("Привет")`. Она показывает текст на экране. Экранный результат после запуска называется выводом. Кавычки не появляются в выводе: они нужны Python только для того, чтобы понять, где начинается и заканчивается текст.

Минимальный пример:

```python
print("Привет")
print("Я пишу код")
```

Первая строка выводит `Привет`. Вторая строка выводит `Я пишу код`. Python не выбирает строки сам и не выполняет их случайно: он идёт сверху вниз.

Что важно: автопроверка сравнивает вывод точно. Для неё `Привет`, `привет` и `Привет ` с пробелом в конце — разные результаты. Перед отправкой смотри не только на смысл, но и на регистр, пробелы и лишние строки.

Частая ошибка: добавить пояснение в вывод, хотя условие просит только точный текст. Если нужно вывести `Привет`, строка `Ответ: Привет` уже не подходит.

Самопроверка: сколько строк появится на экране, если в коде две команды `print(...)`?

### 002. print (theory)

`print()` — это команда Python для вывода. Слово `print` говорит, что нужно что-то показать. Скобки говорят, какие данные получает команда. Кавычки говорят, что внутри находится обычный текст.

Разберём строку:

```python
print("Код работает")
```

`print` — имя команды. Открывающая скобка `(` начинает место для данных. Текст `Код работает` записан в кавычках. Закрывающая скобка `)` завершает команду. После запуска Python выводит:

```text
Код работает
```

Кавычки в вывод не попадают. Они нужны только в коде. Если забыть кавычку, Python не поймёт, где текст заканчивается. Если забыть скобку, команда останется незавершённой. Ошибка в таком месте не означает, что “всё сломано”. Это сигнал: Python не смог прочитать команду.

Самые частые ошибки в первом уроке: пропущена кавычка, пропущена скобка, лишний `print`, лишний текст внутри кавычек или строки стоят не в том порядке.

Что важно: когда условие просит вывести одну строку, не добавляй приветствие, пояснение или пустую строку. Пиши ровно тот вывод, который указан в примере.

Самопроверка: что увидит экран после запуска `print("Python")` — слово с кавычками или без кавычек?

Частая ошибка: читать пример только глазами и не запускать его. Запусти маленький фрагмент и сравни вывод с тем, что ожидаешь увидеть.

### 003. print и вывод (test)

Проверь понимание перед практикой по теме `Первый код`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что делает команда `print("Привет")`?
- Q: Что появится в выводе после `print("Кот")`?
- Q: Почему порядок строк с `print` важен?

### 004. Первый вывод (practice)

**Коротко:** выведи одну точную строку.

### Условие
Напиши одну команду `print(...)`, которая выводит ровно `Привет, Python!`. Не добавляй пояснения, пустые строки или другой текст.

### Вход
Нет входных данных.

### Выход
Одна строка: `Привет, Python!`.

### Пример
Вывод:
```text
Привет, Python!
```

### Что проверить
Сравни вывод с примером. Проверь заглавную букву, запятую, пробел после запятой и восклицательный знак.

### 005. Две строки (practice)

**Коротко:** выведи две строки в заданном порядке.

### Условие
Напиши две команды `print(...)`. Первая строка вывода должна быть `Старт`, вторая — `Готово`.

### Вход
Нет входных данных.

### Выход
Две строки: сначала `Старт`, затем `Готово`.

### Пример
Вывод:
```text
Старт
Готово
```

### Что проверить
В выводе должно быть ровно две строки. Если строки поменять местами, проверка не примет ответ.

### 006. Порядок (practice)

**Коротко:** поставь строки вывода в правильном порядке.

### Условие
В редакторе уже есть три команды. Переставь их так, чтобы программа вывела `Раз`, потом `Два`, потом `Три`.

### Вход
Нет входных данных.

### Выход
Три строки в указанном порядке.

### Пример
Вывод:
```text
Раз
Два
Три
```

### Что проверить
Не меняй текст внутри кавычек. Исправить нужно только порядок команд.

### 007. Точный текст (practice)

**Коротко:** исправь вывод до точного совпадения.

### Условие
Программа должна вывести ровно `Python любит точность`. В стартовом коде текст отличается от нужного. Исправь только текст в кавычках.

### Вход
Нет входных данных.

### Выход
Одна строка: `Python любит точность`.

### Пример
Вывод:
```text
Python любит точность
```

### Что проверить
Проверь регистр первой буквы, пробелы между словами и отсутствие лишней точки в конце.

### 008. Кавычка (practice)

**Коротко:** исправь пропущенную кавычку.

### Условие
В редакторе команда написана с ошибкой: Python не понимает, где заканчивается текст. Исправь строку так, чтобы она вывела `Код работает`.

### Вход
Нет входных данных.

### Выход
Одна строка: `Код работает`.

### Пример
Вывод:
```text
Код работает
```

### Что проверить
Открой и закрой кавычки вокруг текста. После текста должна быть закрывающая скобка.

### 009. Лишняя строка (practice)

**Коротко:** убери лишний вывод.

### Условие
В стартовом коде программа выводит две строки, а нужна только одна: `Готово`. Удали или исправь лишнюю команду так, чтобы остался точный вывод.

### Вход
Нет входных данных.

### Выход
Одна строка: `Готово`.

### Пример
Вывод:
```text
Готово
```

### Что проверить
Если в выводе останется `Почти`, проверка не примет ответ. Нужна только строка из примера.

### 010. Три строки (project)

**Коротко:** создай файл с тремя командами вывода.

### Условие
Создай файл `main.py`. В нём должны быть три команды `print(...)`.

Программа выводит строки строго в таком порядке:

```text
Я пишу код
Python читает строки
Вывод должен совпасть
```

Добавь файл `README.md` с одной фразой: `Запуск: python main.py`.

### Вход
Нет входных данных.

### Выход
Три строки в указанном порядке.

### Пример
Вывод:
```text
Я пишу код
Python читает строки
Вывод должен совпасть
```

### Что проверить
Запусти файл и сравни порядок строк. Не добавляй заголовки, пояснения или пустые строки.

### 011. Короткая проверка (summary)

Перед следующим уроком проверь три простых правила.

1. `print("Текст")` выводит текст без кавычек.
2. Python выполняет строки сверху вниз.
3. Проверка сравнивает вывод точно: регистр, пробелы, порядок строк и лишний текст имеют значение.

Если программа не запускается, сначала смотри на кавычки и скобки. Если программа запускается, но ответ не принимается, сравни вывод с примером посимвольно.

## 002. Ввод и числа

Estimated hours: 2.5
Topics: Python Core

### 001. Что это (theory)

Во втором уроке появляется ввод данных. До этого программа сама решала, что вывести. Теперь она сможет дождаться значения от человека и использовать его в коде.

Команда `input()` читает одну строку. Важно: результат `input()` всегда строка, даже если человек ввёл цифры. Если ввести `2`, Python получает текст `"2"`, а не число `2`. Поэтому выражение `"2" + "3"` даёт строку `"23"`, потому что для текста знак `+` склеивает строки.

Чтобы считать целое число, используют `int(input())`. Сначала `input()` получает строку, потом `int(...)` превращает её в число. Если внутри не число, например `кот`, Python выдаст ошибку. Это нормально: программа не может сложить число и слово без понятного правила.

Для дробных чисел используют `float(...)`: например `2.5`. Арифметика работает с числами: `+`, `-`, `*`, `/`, `//`, `%`.

Что важно: сначала прочитай данные, потом преобразуй тип, потом делай вычисление. Не смешивай текст и числа в одной операции без преобразования.

Самопроверка: почему `input()` после ввода `7` ещё не даёт число для сложения?

Минимальный пример для этой части:

```python
raw = input()
number = int(raw)
print(number + 1)
```

Разбор: первая строка получает текст, вторая превращает текст в число, третья выполняет арифметику.

Частая ошибка: читать пример только глазами и не запускать его. Запусти маленький фрагмент и сравни вывод с тем, что ожидаешь увидеть.

### 002. Минимальный пример (theory)

Минимальная программа с вводом:

```python
name = input()
print("Привет, " + name)
```

Первая строка ждёт ввод и сохраняет его в переменную `name`. Переменная — это имя для значения, чтобы использовать его позже. Вторая строка склеивает текст `Привет, ` и введённое имя, затем выводит результат.

Пример с числом:

```python
age = int(input())
print(age + 1)
```

Сначала `input()` получает строку. Потом `int(...)` превращает её в целое число. После этого `age + 1` работает как арифметика, а не как склейка текста.

Два числа в одной строке читают так:

```python
a, b = input().split()
a = int(a)
b = int(b)
print(a + b)
```

`split()` делит строку по пробелам. Затем каждую часть нужно отдельно превратить в число.

Частая ошибка: написать `age = input()` и потом `print(age + 1)`. Python не складывает строку и число. Если видишь такую ошибку, проверь, где должен быть `int(...)` или `float(...)`.

Самопроверка: что получится у `"2" + "3"` и что получится у `int("2") + int("3")`?

### 003. Ввод и числа (test)

Проверь понимание перед практикой по теме `Ввод и числа`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что возвращает `input()` после ввода `42`?
- Q: Почему `"2" + "3"` даёт `"23"`?
- Q: В каком порядке читать два целых числа из строки `5 8`?

### 004. Имя (practice)

**Коротко:** прочитай имя и выведи приветствие.

### Условие
Прочитай одну строку через `input()`. Выведи `Привет, ` и введённое имя. Используй склейку строк через `+`.

### Вход
Одна строка с именем.

### Выход
Одна строка приветствия.

### Пример
Ввод:
```text
Анна
```
Вывод:
```text
Привет, Анна
```

### Что проверить
Проверь запятую и пробел после неё. Имя нужно взять из ввода без изменения.

### Ограничения
В этой задаче важен именно шаг `Имя` из урока `Ввод и числа`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Возраст (practice)

**Коротко:** прибавь единицу к введённому возрасту.

### Условие
Прочитай возраст, преврати его в целое число и выведи возраст через год. Без `int()` программа будет работать со строкой, а не с числом.

### Вход
Одно целое число.

### Выход
Одно целое число.

### Пример
Ввод:
```text
17
```
Вывод:
```text
18
```

### Что проверить
Проверь, что сложение происходит после `int(input())`, а не со строкой.

### Ограничения
В этой задаче важен именно шаг `Возраст` из урока `Ввод и числа`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Сумма (practice)

**Коротко:** сложи два числа из одной строки.

### Условие
На вход подаются два целых числа через пробел. Раздели строку через `split()`, преврати обе части в числа и выведи сумму.

### Вход
Два целых числа через пробел.

### Выход
Одно целое число.

### Пример
Ввод:
```text
2 3
```
Вывод:
```text
5
```

### Что проверить
Проверь, что обе части преобразованы через `int()`. Если преобразовать только первую часть, сложение не получится.

### Ограничения
В этой задаче важен именно шаг `Сумма` из урока `Ввод и числа`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. Площадь (practice)

**Коротко:** посчитай площадь прямоугольника.

### Условие
Прочитай ширину и высоту как дробные числа: каждое значение приходит на отдельной строке. Выведи произведение ширины и высоты.

### Вход
Два числа, каждое на своей строке.

### Выход
Одно число.

### Пример
Ввод:
```text
3.5
2
```
Вывод:
```text
7.0
```

### Что проверить
Используй `float(input())` для обоих значений. Вывод `7.0` подходит, потому что вычисление идёт с дробными числами.

### Ограничения
В этой задаче важен именно шаг `Площадь` из урока `Ввод и числа`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Скидка (practice)

**Коротко:** вычти десятую часть цены.

### Условие
Прочитай целую цену. Посчитай цену после скидки 10 процентов: вычти `price // 10`. Выведи итоговое целое число.

### Вход
Одно целое число.

### Выход
Одно целое число.

### Пример
Ввод:
```text
1000
```
Вывод:
```text
900
```

### Что проверить
Проверь, что используешь целочисленное деление `//`. В этой задаче копейки не нужны.

### Ограничения
В этой задаче важен именно шаг `Скидка` из урока `Ввод и числа`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Забытый int (practice)

**Коротко:** исправь сложение строки и числа.

### Условие
В стартовом коде год читается как строка, а потом к нему прибавляется число. Исправь код так, чтобы программа вывела следующий год.

### Вход
Одно целое число.

### Выход
Следующий год.

### Пример
Ввод:
```text
2026
```
Вывод:
```text
2027
```

### Что проверить
Не меняй смысл программы. Нужно добавить преобразование к числу перед арифметикой.

### Ограничения
В этой задаче важен именно шаг `Забытый int` из урока `Ввод и числа`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 010. Следующий год (project)

**Коротко:** собери маленькую программу, которая читает год и выводит следующий.

### Условие
Создай файл `main.py`. Программа читает один год, превращает ввод в целое число и выводит следующий год.

Создай `README.md` с примером запуска: ввод `2026`, вывод `2027`.

### Вход
Одно целое число.

### Выход
Одно целое число — следующий год.

### Пример
Ввод:
```text
2026
```
Вывод:
```text
2027
```

### Что проверить
Запусти программу с разными годами. Если видишь ошибку сложения строки и числа, проверь `int(input())`.

### 011. Короткая проверка (summary)

Держи рядом короткий список правил.

`input()` даёт строку. Для целых чисел нужен `int(...)`, для дробных — `float(...)`. Два числа в одной строке сначала разделяют через `split()`, потом каждую часть превращают в число. Арифметика работает после преобразования.

Если вывод похож на склеенный текст, проверь, не осталось ли число строкой. Если программа падает на вводе, проверь, действительно ли во входных данных число.

## 003. Строки

Estimated hours: 2.5
Topics: Python Core

### 001. Что это (theory)

Строка — это текстовое значение. Имя, сообщение, адрес страницы, команда из консоли, строка из файла — всё это часто попадает в программу как текст. В Python строку записывают в кавычках: `"hello"` или `"Привет"`.

Строку можно читать через `input()`, сохранять в переменную и обрабатывать. У строки есть позиции символов. Позиция называется индексом. Первый символ имеет индекс `0`, а не `1`. Это непривычно, но важно: `text[0]` берёт первый символ.

Срез берёт часть строки: `text[0:3]` берёт символы с позиции `0` до позиции `3`, не включая позицию `3`. Методы строк помогают чистить и менять текст: `strip()` убирает пробелы по краям, `lower()` переводит буквы в нижний регистр, `replace()` заменяет один фрагмент на другой, `split()` делит строку на части.

Что важно: строка не меняется сама. Метод возвращает новую строку. Если написать `name.strip()`, но не сохранить результат, переменная `name` останется прежней.

Частая ошибка: вызвать метод и забыть присваивание. Правильно так: `name = name.strip()`.

Самопроверка: почему у первого символа индекс `0`, а не `1`, нужно просто запомнить и проверять на маленьких примерах.

Минимальный пример для этой части:

```python
text = input()
print(text.strip())
```

Разбор: первая строка получает текст, вторая выводит новую строку без пробелов по краям.

### 002. Минимальный пример (theory)

Разберём маленькую программу:

```python
text = input()
clean = text.strip().lower()
print(clean)
```

Первая строка читает текст. Вторая строка делает две операции: `strip()` убирает пробелы по краям, затем `lower()` приводит буквы к нижнему регистру. Результат сохраняется в новую переменную `clean`. Третья строка выводит очищенный текст.

Пример со срезом:

```python
word = input()
print(word[0:3])
```

Если ввести `python`, вывод будет `pyt`. Срез `0:3` берёт позиции `0`, `1` и `2`.

Пример с заменой:

```python
title = input()
print(title.replace(" ", "-"))
```

Пробелы заменяются дефисами. Это похоже на подготовку коротких адресов страниц.

Частая ошибка: ожидать, что `text.strip()` изменит `text` без присваивания. Строки в Python неизменяемые: метод возвращает новый результат.

Самопроверка: что выведет `print("Python"[0:2])`?

### 003. Строки (test)

Проверь понимание перед практикой по теме `Строки`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что вернёт `"Python"[0]`?
- Q: Почему после `name.strip()` переменная `name` может остаться с пробелами?
- Q: Что делает `replace(" ", "-")`?

### 004. Без краёв (practice)

**Коротко:** убери пробелы по краям строки.

### Условие
Прочитай строку, примени `strip()` и выведи очищенный текст. Пробелы внутри текста трогать не нужно.

### Вход
Одна строка.

### Выход
Та же строка без пробелов по краям.

### Пример
Ввод:
```text
  Анна
```
Вывод:
```text
Анна
```

### Что проверить
Проверь, что результат `strip()` сразу выводится или сохраняется перед выводом.

### Ограничения
В этой задаче важен именно шаг `Без краёв` из урока `Строки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Нижний регистр (practice)

**Коротко:** приведи текст к нижнему регистру.

### Условие
Прочитай строку и выведи её через `lower()`. Пробелы не меняй.

### Вход
Одна строка.

### Выход
Строка в нижнем регистре.

### Пример
Ввод:
```text
PyThOn
```
Вывод:
```text
python
```

### Что проверить
Проверь, что используешь метод `lower()`, а не меняешь только одну букву руками.

### Ограничения
В этой задаче важен именно шаг `Нижний регистр` из урока `Строки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Первые символы (practice)

**Коротко:** возьми первые три символа строки.

### Условие
Прочитай строку и выведи срез от начала до третьей позиции. Если строка короче трёх символов, Python просто вернёт всю доступную часть.

### Вход
Одна строка.

### Выход
До трёх первых символов.

### Пример
Ввод:
```text
backend
```
Вывод:
```text
bac
```

### Что проверить
Проверь границы среза: начало `0`, конец `3`. Символ с позицией `3` в результат не входит.

### Ограничения
В этой задаче важен именно шаг `Первые символы` из урока `Строки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. Замена (practice)

**Коротко:** замени пробелы дефисами.

### Условие
Прочитай строку и замени каждый пробел на `-` через `replace`. Остальные символы оставь как есть.

### Вход
Одна строка.

### Выход
Строка с дефисами вместо пробелов.

### Пример
Ввод:
```text
python backend
```
Вывод:
```text
python-backend
```

### Что проверить
Проверь, что заменяются именно пробелы, а не все символы.

### Ограничения
В этой задаче важен именно шаг `Замена` из урока `Строки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Slug (practice)

**Коротко:** собери короткий адрес из заголовка.

### Условие
Прочитай заголовок. Убери пробелы по краям, приведи к нижнему регистру, раздели на части через `split()` и склей части дефисом через `join`.

### Вход
Одна строка.

### Выход
Одна строка для адреса.

### Пример
Ввод:
```text
  Python Backend
```
Вывод:
```text
python-backend
```

### Что проверить
Проверь порядок операций: сначала очистка краёв, потом нижний регистр, потом деление и склейка.

### Ограничения
В этой задаче важен именно шаг `Slug` из урока `Строки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Забыто присваивание (practice)

**Коротко:** исправь метод без сохранения результата.

### Условие
В стартовом коде вызывается `strip()`, но результат не сохраняется. Исправь код так, чтобы вывод был без пробелов по краям.

### Вход
Одна строка.

### Выход
Очищенная строка.

### Пример
Ввод:
```text
  кот
```
Вывод:
```text
кот
```

### Что проверить
Не нужно добавлять новые методы. Нужно сохранить результат `strip()` в переменную или сразу вывести его.

### Ограничения
В этой задаче важен именно шаг `Забыто присваивание` из урока `Строки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 010. Чистое имя (project)

**Коротко:** собери программу для очистки имени.

### Условие
Создай `main.py`. Программа читает имя, убирает пробелы по краям и переводит буквы в нижний регистр.

Создай `README.md` с примером: ввод `  ANNA  `, вывод `anna`.

### Вход
Одна строка с именем.

### Выход
Очищенная строка в нижнем регистре.

### Пример
Ввод:
```text
  ANNA
```
Вывод:
```text
anna
```

### Что проверить
Методы строк возвращают новое значение. Если пробелы остались, проверь, сохранён ли результат `strip()`.

### 011. Короткая проверка (summary)

Главные правила строк:

индекс `0` берёт первый символ; срез `0:3` берёт первые три символа; `strip`, `lower`, `replace` и другие методы возвращают новую строку. Если метод вызван, но результат не сохранён и не выведен, исходная переменная не изменится.

Когда строковая задача не проходит, проверь края строки, регистр, лишние пробелы и порядок методов.

## 004. bool и None

Estimated hours: 2.2
Topics: Python Core

### 001. Что это (theory)

`bool` — это тип для двух ответов: `True` и `False`. Такие значения появляются после сравнений. Например, `5 > 3` даёт `True`, а `5 == 3` даёт `False`. В backend-коде такие проверки встречаются постоянно: заполнено ли поле, совпал ли пароль, есть ли доступ, найден ли объект.

`None` означает отсутствие значения. Это не строка `"None"`, не ноль и не пустая строка. `None` используют, когда значения пока нет или функция позже явно вернёт отсутствие результата.

Сравнения пишутся так: `==` проверяет равенство, `!=` проверяет неравенство, `>`, `<`, `>=`, `<=` сравнивают порядок чисел. Для проверки `None` используют `is None`, потому что так код яснее показывает намерение.

Есть ещё идея truthy и falsy: пустая строка после очистки считается ложной, непустая — истинной. Но `0`, пустая строка и `None` — разные вещи. Не смешивай их без причины.

Частая ошибка: проверять `value == None`. Работать может, но в Python принято писать `value is None`.

Самопроверка: чем отличается пустая строка `""` от `None`?

Минимальный пример для этой части:

```python
# bool и None: концептуальный разбор
# термин -> граница -> проверка
value = None
print(value is None)
```

Разбор: первая строка создаёт отсутствие значения, вторая проверяет именно это состояние.

### 002. Минимальный пример (theory)

Минимальный пример со сравнением:

```python
age = int(input())
print(age >= 18)
```

Первая строка читает число. Вторая строка сравнивает его с `18` и выводит результат сравнения: `True` или `False`. Здесь пока нет выбора ветки программы, только сам ответ сравнения.

Пример с пустой строкой:

```python
text = input().strip()
print(bool(text))
```

`strip()` убирает пробелы по краям. `bool(text)` показывает, считается ли строка заполненной. Пустая строка даст `False`, непустая — `True`.

Пример с `None`:

```python
value = None
print(value is None)
```

Так проверяют именно отсутствие значения. Если написать строку `"None"`, это будет обычный текст, а не специальное значение.

Частая ошибка: думать, что `0`, `""` и `None` всегда одно и то же. Они могут вести себя как ложные значения, но смысл у них разный.

Самопроверка: что выведет `print(10 == 10)`?

### 003. bool и None (test)

Проверь понимание перед практикой по теме `bool и None`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что вернёт выражение `7 > 3`?
- Q: Как в Python обычно проверяют отсутствие значения?
- Q: Почему строка `"None"` не равна `None`?

### 004. Равно (practice)

**Коротко:** сравни два числа на равенство.

### Условие
Прочитай два целых числа из одной строки. Выведи результат `a == b`.

### Вход
Два целых числа через пробел.

### Выход
`True` или `False`.

### Пример
Ввод:
```text
5 5
```
Вывод:
```text
True
```

### Что проверить
Проверь, что сравниваешь числа после `int()`, а не строки.

### Ограничения
В этой задаче важен именно шаг `Равно` из урока `bool и None`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Положительное (practice)

**Коротко:** проверь, что число больше нуля.

### Условие
Прочитай целое число и выведи результат сравнения `number > 0`.

### Вход
Одно целое число.

### Выход
`True` или `False`.

### Пример
Ввод:
```text
8
```
Вывод:
```text
True
```

### Что проверить
Проверь граничный случай: `0 > 0` даёт `False`.

### Ограничения
В этой задаче важен именно шаг `Положительное` из урока `bool и None`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Пустая строка (practice)

**Коротко:** проверь, заполнен ли текст после очистки.

### Условие
Прочитай строку, убери пробелы по краям и выведи `bool(text)`. Пустая после очистки строка должна дать `False`.

### Вход
Одна строка.

### Выход
`True` или `False`.

### Пример
Ввод:
```text
  hello
```
Вывод:
```text
True
```

### Что проверить
Проверь ввод из одних пробелов: после `strip()` он становится пустой строкой.

### Ограничения
В этой задаче важен именно шаг `Пустая строка` из урока `bool и None`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. None (practice)

**Коротко:** проверь отсутствие значения.

### Условие
Создай переменную `value = None` и выведи результат проверки `value is None`.

### Вход
Нет входных данных.

### Выход
`True`.

### Пример
Вывод:
```text
True
```

### Что проверить
`None` пишется без кавычек. Строка `"None"` не подходит.

### Ограничения
В этой задаче важен именно шаг `None` из урока `bool и None`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Не строка None (practice)

**Коротко:** исправь путаницу между текстом и отсутствием значения.

### Условие
В стартовом коде переменная `missing` содержит строку `"None"`, поэтому проверка отсутствия значения даёт неправильный ответ. Исправь код так, чтобы вывод был `True`.

### Вход
Нет входных данных.

### Выход
`True`.

### Пример
Вывод:
```text
True
```

### Что проверить
Убери кавычки вокруг `None`. Нужен не текст, а специальное значение.

### Ограничения
В этой задаче важен именно шаг `Не строка None` из урока `bool и None`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Заполнено (project)

**Коротко:** собери программу, которая проверяет заполненность строки.

### Условие
Создай `main.py`. Программа читает одну строку, убирает пробелы по краям и выводит `True`, если после очистки остался текст, иначе `False`.

Создай `README.md` с примером: ввод `  mail  `, вывод `True`.

### Вход
Одна строка.

### Выход
`True` или `False`.

### Пример
Ввод:
```text
  mail
```
Вывод:
```text
True
```

### Что проверить
Отдельно проверь строку из одних пробелов. После очистки она должна считаться пустой.

### 010. Короткая проверка (summary)

Булевы значения появляются после сравнений: `==`, `>`, `<`, `>=`, `<=`. `None` означает отсутствие значения и проверяется через `is None`. Пустая строка, ноль и `None` могут вести себя как ложные значения, но смысл у них разный.

Если проверка выглядит странно, сначала выясни тип значения: текст это, число или `None`.

## 005. Условия

Estimated hours: 2.4
Topics: Python Core

### 001. Что это (theory)

Условие позволяет программе выбрать, какие строки выполнить. До этого код шёл прямо сверху вниз. Теперь можно сказать: если проверка истинна, выполни один блок, иначе выполни другой.

В Python условие начинается с `if`. После проверки ставится двоеточие. Строки внутри ветки пишутся с отступом. Отступ — это часть синтаксиса, а не украшение. По отступу Python понимает, какие команды относятся к условию.

Пример задачи из обычной жизни: прочитать возраст и вывести, можно ли войти. Если возраст 18 или больше, выводим `Можно`. Иначе выводим `Нельзя`.

`else` задаёт ветку для всех остальных случаев. `elif` добавляет ещё одну проверку между `if` и `else`. Порядок проверок важен: Python выбирает первую подходящую ветку.

Частая ошибка: написать `=` вместо `==`. Один знак равенства присваивает значение, два знака сравнивают значения. В условии для проверки равенства нужен `==`.

Самопроверка: почему строки внутри `if` должны быть с одинаковым отступом?

Минимальный пример для этой части:

```python
# Условия: концептуальный разбор
# термин -> граница -> проверка
age = int(input())
if age >= 18:
    print("Можно")
else:
    print("Нельзя")
```

Разбор: проверка выбирает одну из двух веток, а отступ показывает команды внутри веток.

### 002. Минимальный пример (theory)

Разберём программу:

```python
age = int(input())
if age >= 18:
    print("Можно")
else:
    print("Нельзя")
```

Первая строка читает возраст как число. Строка `if age >= 18:` проверяет условие. Если оно истинно, выполняется команда с отступом под `if`: вывод `Можно`. Если условие ложно, Python переходит к `else` и выполняет команду под ним.

Пример с тремя ветками:

```python
number = int(input())
if number > 0:
    print("плюс")
elif number == 0:
    print("ноль")
else:
    print("минус")
```

Сначала проверяется положительное число. Потом отдельно проверяется ноль. Всё остальное попадает в `else`.

Что важно: границы нужно читать внимательно. Условие `age >= 18` включает 18. Условие `age > 18` не включает 18.

Частая ошибка: поставить правильное условие, но забыть отступ у строки `print`.

Самопроверка: что выведет первая программа при вводе `18`?

### 003. Условия (test)

Проверь понимание перед практикой по теме `Условия`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что означает отступ под строкой `if age >= 18:`?
- Q: Как проверить равенство двух значений?
- Q: Какая проверка включает возраст 18?

### 004. Возраст (practice)

**Коротко:** выбери ответ по возрасту.

### Условие
Прочитай возраст. Если возраст 18 или больше, выведи `Можно`. Иначе выведи `Нельзя`.

### Вход
Одно целое число.

### Выход
Одна строка.

### Пример
Ввод:
```text
17
```
Вывод:
```text
Нельзя
```

### Что проверить
Проверь граничный случай: при вводе `18` должен быть вывод `Можно`.

### Ограничения
В этой задаче важен именно шаг `Возраст` из урока `Условия`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Знак числа (practice)

**Коротко:** раздели число на три случая.

### Условие
Прочитай целое число. Выведи `плюс`, если число больше нуля; `ноль`, если равно нулю; `минус` во всех остальных случаях.

### Вход
Одно целое число.

### Выход
Одна строка.

### Пример
Ввод:
```text
0
```
Вывод:
```text
ноль
```

### Что проверить
Проверь порядок веток: сначала положительное число, потом ноль, потом оставшийся случай.

### Ограничения
В этой задаче важен именно шаг `Знак числа` из урока `Условия`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Граница (practice)

**Коротко:** проверь проходной балл.

### Условие
Прочитай количество баллов. Если баллов 60 или больше, выведи `pass`. Иначе выведи `fail`.

### Вход
Одно целое число.

### Выход
`pass` или `fail`.

### Пример
Ввод:
```text
60
```
Вывод:
```text
pass
```

### Что проверить
Граница включена. Значит, 60 должно пройти.

### Ограничения
В этой задаче важен именно шаг `Граница` из урока `Условия`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. Код доступа (practice)

**Коротко:** сравни введённый код с ожидаемым.

### Условие
Прочитай строку. Если она равна `OPEN`, выведи `Вход`. Иначе выведи `Отказ`.

### Вход
Одна строка.

### Выход
Одна строка.

### Пример
Ввод:
```text
OPEN
```
Вывод:
```text
Вход
```

### Что проверить
Проверь регистр. `open` и `OPEN` — разные строки.

### Ограничения
В этой задаче важен именно шаг `Код доступа` из урока `Условия`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Один знак (practice)

**Коротко:** исправь присваивание вместо сравнения.

### Условие
В стартовом коде в условии использован один знак `=`, поэтому Python не может прочитать проверку. Исправь код так, чтобы он сравнивал введённый код с `OK`.

### Вход
Одна строка.

### Выход
`yes` или `no`.

### Пример
Ввод:
```text
OK
```
Вывод:
```text
yes
```

### Что проверить
Не меняй ветки вывода. Нужно исправить саму проверку.

### Ограничения
В этой задаче важен именно шаг `Один знак` из урока `Условия`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Можно войти (project)

**Коротко:** собери программу проверки возраста.

### Условие
Создай `main.py`. Программа читает возраст. Если возраст 18 или больше, выводит `Можно войти`. Иначе выводит `Рано`.

Создай `README.md` с двумя примерами: для `17` и для `18`.

### Вход
Одно целое число.

### Выход
Одна строка.

### Пример
Ввод:
```text
18
```
Вывод:
```text
Можно войти
```

### Что проверить
Обязательно проверь границу `18`. Если используешь `> 18`, программа ошибётся на главном случае.

### 010. Короткая проверка (summary)

Условие начинается с `if`, после проверки ставится двоеточие, команды ветки пишутся с отступом. `elif` добавляет промежуточную проверку, `else` ловит остальные случаи. Для равенства нужен `==`, для включённой границы — `>=` или `<=`.

Если код не запускается, проверь двоеточие и отступы. Если запускается, но даёт неверный ответ, проверь порядок веток и граничные значения.

## 006. Типы и память

Estimated hours: 2.2
Topics: Python Core

### 001. Что это (theory)

У каждого значения в Python есть тип. Тип отвечает на вопрос: что это за значение и какие операции с ним возможны. `"42"` — строка, `42` — целое число, `42.0` — дробное число, `True` — булево значение.

Переменная хранит ссылку на значение. Когда ты пишешь `x = 10`, имя `x` начинает указывать на число `10`. Если потом написать `x = 20`, имя `x` начинает указывать на другое значение. Старое число не “переписывается” внутри переменной, просто имя связано с новым значением.

Функция `type(value)` показывает тип значения. Для вывода удобного имени можно использовать `type(value).__name__`. Это помогает диагностировать ошибки: например, понять, почему вместо сложения чисел получилась склейка строк.

Что важно: преобразование создаёт новое значение другого типа. `int("42")` не меняет строку `"42"`, а возвращает число `42`.

Частая ошибка: считать, что переменная навсегда привязана к первому типу. В Python имя можно связать с другим значением, но читать такой код становится сложнее.

Самопроверка: чем отличается `"5"` от `5`?

Минимальный пример для этой части:

```python
raw = input()
print(type(raw).__name__)
```

Разбор: ввод сохраняется как строка, а `type(...).__name__` показывает короткое имя типа.

### 002. Минимальный пример (theory)

Разберём программу:

```python
value = input()
print(type(value).__name__)
number = int(value)
print(type(number).__name__)
```

Первая строка читает ввод. Даже если введено `123`, переменная `value` содержит строку. Поэтому первый вывод будет `str`. Затем `int(value)` создаёт целое число и сохраняет его в `number`. Второй вывод будет `int`.

Пример с переприсваиванием:

```python
name = "Анна"
name = "Олег"
print(name)
```

Вывод будет `Олег`, потому что вторая строка связала имя `name` с новым значением.

Пример с двумя именами:

```python
first = "кот"
second = first
first = "пёс"
print(second)
```

Вывод будет `кот`. Имя `second` сохранило ссылку на старое строковое значение.

Частая ошибка: смотреть только на имя переменной и забывать, какое значение и какой тип сейчас внутри.

Самопроверка: какой тип у результата `float("2.5")`?

### 003. Типы (test)

Проверь понимание перед практикой по теме `Типы и память`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что выведет `type(input()).__name__` после ввода `123`?
- Q: Что делает `int("42")`?
- Q: Что выведет код `x = "a"; x = "b"; print(x)`?

### 004. Имя типа (practice)

**Коротко:** покажи тип значения после ввода.

### Условие
Прочитай любое значение через `input()` и выведи имя его типа через `type(value).__name__`.

### Вход
Одна строка.

### Выход
`str`.

### Пример
Ввод:
```text
123
```
Вывод:
```text
str
```

### Что проверить
Даже цифры из `input()` сначала являются строкой.

### Ограничения
В этой задаче важен именно шаг `Имя типа` из урока `Типы и память`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. После int (practice)

**Коротко:** покажи тип после преобразования.

### Условие
Прочитай целое число через `int(input())` и выведи имя типа получившегося значения.

### Вход
Одно целое число.

### Выход
`int`.

### Пример
Ввод:
```text
42
```
Вывод:
```text
int
```

### Что проверить
Проверь, что `type()` получает уже преобразованное значение.

### Ограничения
В этой задаче важен именно шаг `После int` из урока `Типы и память`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. После float (practice)

**Коротко:** покажи тип дробного числа.

### Условие
Прочитай число через `float(input())` и выведи имя типа.

### Вход
Одно число.

### Выход
`float`.

### Пример
Ввод:
```text
2.5
```
Вывод:
```text
float
```

### Что проверить
Целый ввод вроде `2` тоже можно превратить в `float`; тип всё равно будет `float`.

### Ограничения
В этой задаче важен именно шаг `После float` из урока `Типы и память`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. Переприсваивание (practice)

**Коротко:** исправь порядок присваивания.

### Условие
В стартовом коде переменная меняется несколько раз. Исправь порядок строк так, чтобы программа вывела `10`.

### Вход
Нет входных данных.

### Выход
Одна строка: `10`.

### Пример
Вывод:
```text
10
```

### Что проверить
Последнее присваивание перед выводом определяет значение, которое попадёт на экран.

### Ограничения
В этой задаче важен именно шаг `Переприсваивание` из урока `Типы и память`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Два имени (practice)

**Коротко:** проверь, что второе имя сохранило старое значение.

### Условие
В стартовом коде есть переменные `first` и `second`. Измени только порядок вывода так, чтобы программа показала значение `second` после переприсваивания `first`.

### Вход
Нет входных данных.

### Выход
Одна строка: `кот`.

### Пример
Вывод:
```text
кот
```

### Что проверить
`second` получил значение до того, как `first` связали с новым текстом.

### Ограничения
В этой задаче важен именно шаг `Два имени` из урока `Типы и память`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Паспорт значения (project)

**Коротко:** собери программу, которая показывает значение и его тип после преобразования.

### Условие
Создай `main.py`. Программа читает целое число как строку, выводит тип исходного значения, затем превращает его в число и выводит тип числа.

Создай `README.md` с примером: ввод `15`, вывод `str`, затем `int`.

### Вход
Одно целое число.

### Выход
Две строки: тип до преобразования и тип после преобразования.

### Пример
Ввод:
```text
15
```
Вывод:
```text
str
int
```

### Что проверить
Сначала сохрани ввод без преобразования. Потом создай отдельную переменную для числа.

### 010. Короткая проверка (summary)

Тип показывает, какие операции подходят значению. `input()` даёт `str`; `int()` и `float()` создают числовые значения. Переменная — имя, связанное со значением. Новое присваивание связывает имя с новым значением.

Если результат операции странный, проверь тип через `type(value).__name__` и место, где значение преобразуется.

## 007. Циклы

Estimated hours: 2.7
Topics: Python Core

### 001. Что это (theory)

Цикл нужен, когда одну и ту же команду нужно выполнить несколько раз. Без цикла пришлось бы писать много одинаковых строк. В Python чаще всего используют `for` и `while`.

`for` удобен, когда заранее понятно, сколько повторений нужно. Например, `range(1, 4)` даёт числа `1`, `2`, `3`. Цикл берёт каждое число по очереди и выполняет блок команд с отступом.

`while` удобен, когда повторение зависит от условия. Он выполняет блок, пока условие истинно. Важно следить, чтобы условие когда-нибудь стало ложным, иначе программа может повторяться бесконечно.

`break` останавливает цикл раньше времени. `continue` пропускает оставшуюся часть текущего повторения и переходит к следующему.

Что важно: тело цикла пишется с отступом. Переменная внутри `for` получает новое значение на каждом повторении.

Частая ошибка: забыть изменить значение в `while`. Тогда условие остаётся истинным слишком долго.

Самопроверка: сколько строк выведет цикл по `range(1, 4)`?

Минимальный пример для этой части:

```python
# Циклы: концептуальный разбор
# термин -> граница -> проверка
for number in range(1, 4):
    print(number)
```

Разбор: цикл берёт числа по очереди и выполняет команду вывода для каждого числа.

### 002. Минимальный пример (theory)

Пример с `for`:

```python
for number in range(1, 4):
    print(number)
```

`range(1, 4)` даёт 1, 2 и 3. На первом повторении `number` равен 1, на втором — 2, на третьем — 3. Команда `print(number)` выполняется каждый раз.

Пример суммы:

```python
n = int(input())
total = 0
for number in range(1, n + 1):
    total = total + number
print(total)
```

`total` начинается с нуля. На каждом повторении к нему прибавляется очередное число. После цикла выводится итог.

Пример с `while`:

```python
count = int(input())
while count > 0:
    print(count)
    count = count - 1
```

После каждого вывода значение уменьшается. Поэтому цикл остановится.

Частая ошибка: поставить `print(total)` с отступом внутри цикла, когда нужен только итог после цикла.

Самопроверка: почему `n + 1` нужен в `range(1, n + 1)`?

### 003. Циклы (test)

Проверь понимание перед практикой по теме `Циклы`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Какие числа даёт `range(1, 4)`?
- Q: Зачем перед суммированием пишут `total = 0`?
- Q: Что может случиться, если в `while` не менять значение, от которого зависит условие?

### 004. Range (practice)

**Коротко:** выведи числа от 1 до 3.

### Условие
Напиши цикл `for`, который выводит числа `1`, `2`, `3`, каждое с новой строки.

### Вход
Нет входных данных.

### Выход
Три строки с числами.

### Пример
Вывод:
```text
1
2
3
```

### Что проверить
Используй `range(1, 4)`. Правая граница не входит в результат.

### Ограничения
В этой задаче важен именно шаг `Range` из урока `Циклы`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Сумма (practice)

**Коротко:** найди сумму чисел от 1 до `n`.

### Условие
Прочитай `n`. С помощью цикла `for` посчитай сумму от 1 до `n` включительно и выведи итог.

### Вход
Одно целое число.

### Выход
Одно целое число.

### Пример
Ввод:
```text
5
```
Вывод:
```text
15
```

### Что проверить
Проверь, что итог выводится после цикла, а не на каждом повторении.

### Ограничения
В этой задаче важен именно шаг `Сумма` из урока `Циклы`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Повтор слова (practice)

**Коротко:** выведи слово несколько раз.

### Условие
Прочитай слово и число `n` с двух строк. Выведи слово `n` раз, каждый раз на новой строке.

### Вход
Первая строка — слово, вторая — целое число.

### Выход
Слово на нескольких строках.

### Пример
Ввод:
```text
go
3
```
Вывод:
```text
go
go
go
```

### Что проверить
Проверь, что количество строк равно `n`.

### Ограничения
В этой задаче важен именно шаг `Повтор слова` из урока `Циклы`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. Обратный отсчёт (practice)

**Коротко:** выведи числа от `n` до 1 через `while`.

### Условие
Прочитай `n`. Пока `n` больше нуля, выводи его и уменьшай на 1.

### Вход
Одно целое число.

### Выход
Несколько строк с числами.

### Пример
Ввод:
```text
3
```
Вывод:
```text
3
2
1
```

### Что проверить
Проверь, что внутри цикла значение уменьшается. Без этого цикл не остановится.

### Ограничения
В этой задаче важен именно шаг `Обратный отсчёт` из урока `Циклы`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Stop (practice)

**Коротко:** посчитай строки до слова `stop`.

### Условие
Читай строки в цикле `while True`. Если пришло слово `stop`, останови цикл через `break`. Выведи количество строк до `stop`.

### Вход
Несколько строк, одна из них `stop`.

### Выход
Одно целое число.

### Пример
Ввод:
```text
a
b
stop
```
Вывод:
```text
2
```

### Что проверить
Слово `stop` не считается. Счётчик увеличивается только для обычных строк.

### Ограничения
В этой задаче важен именно шаг `Stop` из урока `Циклы`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Забыто уменьшение (practice)

**Коротко:** исправь цикл, который не меняет условие.

### Условие
В стартовом коде `while` выводит число `count`, но не уменьшает его. Добавь строку изменения так, чтобы программа вывела числа от `n` до 1.

### Вход
Одно целое число.

### Выход
Несколько строк с числами.

### Пример
Ввод:
```text
2
```
Вывод:
```text
2
1
```

### Что проверить
Строка `n = n - 1` должна быть внутри цикла после вывода.

### Ограничения
В этой задаче важен именно шаг `Забыто уменьшение` из урока `Циклы`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 010. Сумма до n (project)

**Коротко:** собери программу суммы от 1 до `n`.

### Условие
Создай `main.py`. Программа читает целое число `n`, считает сумму чисел от 1 до `n` включительно и выводит итог.

Создай `README.md` с примером: ввод `5`, вывод `15`.

### Вход
Одно целое число.

### Выход
Одно целое число.

### Пример
Ввод:
```text
5
```
Вывод:
```text
15
```

### Что проверить
Вывод должен быть один раз после цикла. Если вывод стоит внутри цикла, появится несколько строк.

### 011. Короткая проверка (summary)

`for` подходит для известного количества повторений. `while` подходит для повторения по условию. `break` останавливает цикл. Счётчик или накопитель обычно создают до цикла и меняют внутри.

Если цикл выводит слишком много строк, проверь отступ у `print`. Если цикл не останавливается, проверь, меняется ли значение в условии.

## 008. Списки

Estimated hours: 2.5
Topics: Python Core

### 001. Что это (theory)

Список хранит несколько значений в одном порядке. Например, список покупок, список задач, список строк из файла. В Python список записывают в квадратных скобках: `items = ["хлеб", "молоко"]`.

У каждого элемента есть индекс. Как и в строках, первый индекс — `0`. `items[0]` берёт первый элемент, `items[-1]` — последний. Метод `append()` добавляет элемент в конец списка.

Список можно менять: заменить элемент по индексу, добавить новый, удалить позже в следующих темах. Это отличие от строк: строка не меняется сама, а список изменяемый.

Важно отличать копию от второго имени для того же списка. Если написать `second = first`, оба имени указывают на один список. Если нужен отдельный список, используй `first.copy()`.

Строку можно превратить в список слов через `split()`. Это часто нужно, когда ввод содержит несколько значений.

Частая ошибка: обратиться к индексу, которого нет. Если в списке два элемента, последний индекс — `1`, а не `2`.

Самопроверка: какой индекс у первого элемента списка?

Минимальный пример для этой части:

```python
items = ["api", "data"]
items.append("dev")
print(len(items))
```

Разбор: список хранит несколько значений, `append` добавляет новое, `len` считает элементы.

### 002. Минимальный пример (theory)

Разберём список:

```python
items = ["api", "data"]
items.append("dev")
print(items[0])
print(len(items))
```

Первая строка создаёт список из двух строк. `append("dev")` добавляет третий элемент в конец. `items[0]` берёт первый элемент — `api`. `len(items)` показывает длину списка, теперь это `3`.

Пример изменения:

```python
items = ["черновик", "готово"]
items[0] = "старт"
print(items[0])
```

Список изменяемый, поэтому элемент с индексом `0` можно заменить.

Пример копии:

```python
base = ["a"]
copy = base.copy()
copy.append("b")
print(len(base))
```

Вывод будет `1`, потому что изменяли копию, а не исходный список.

Частая ошибка: написать `items[2]` для списка из двух элементов. Индексы начинаются с нуля, поэтому элементы имеют индексы `0` и `1`.

Самопроверка: что выведет `len(["a", "b"])`?

### 003. Списки (test)

Проверь понимание перед практикой по теме `Списки`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что вернёт `items[0]` для `items = ["a", "b"]`?
- Q: Что делает `append`?
- Q: Зачем нужен `copy()`?

### 004. Первый элемент (practice)

**Коротко:** выведи первый элемент списка.

### Условие
В стартовом коде есть список `items`. Выведи его первый элемент.

### Вход
Нет входных данных.

### Выход
Одна строка.

### Пример
Вывод:
```text
api
```

### Что проверить
Первый индекс — `0`. Индекс `1` взял бы второй элемент.

### Ограничения
В этой задаче важен именно шаг `Первый элемент` из урока `Списки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Append (practice)

**Коротко:** добавь элемент и выведи длину.

### Условие
В стартовом коде есть список из двух элементов. Добавь строку `dev` через `append()` и выведи длину списка.

### Вход
Нет входных данных.

### Выход
Одно число.

### Пример
Вывод:
```text
3
```

### Что проверить
После добавления в списке должно стать три элемента.

### Ограничения
В этой задаче важен именно шаг `Append` из урока `Списки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Замена (practice)

**Коротко:** замени элемент по индексу.

### Условие
В списке первый элемент — черновик. Замени его на `готово` и выведи весь список.

### Вход
Нет входных данных.

### Выход
Строковое представление списка.

### Пример
Вывод:
```text
['готово', 'проверка']
```

### Что проверить
Менять нужно элемент с индексом `0`.

### Ограничения
В этой задаче важен именно шаг `Замена` из урока `Списки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. Слова (practice)

**Коротко:** посчитай слова во введённой строке.

### Условие
Прочитай строку со словами. Раздели её через `split()` и выведи количество получившихся слов.

### Вход
Одна строка.

### Выход
Одно целое число.

### Пример
Ввод:
```text
python backend api
```
Вывод:
```text
3
```

### Что проверить
`split()` без аргументов корректно обрабатывает несколько пробелов между словами.

### Ограничения
В этой задаче важен именно шаг `Слова` из урока `Списки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Копия (practice)

**Коротко:** исправь изменение исходного списка.

### Условие
В стартовом коде второе имя указывает на тот же список, поэтому исходный список меняется. Сделай отдельную копию перед добавлением элемента.

### Вход
Нет входных данных.

### Выход
Одно число — длина исходного списка.

### Пример
Вывод:
```text
1
```

### Что проверить
Используй `copy()`, чтобы добавление в новый список не меняло исходный.

### Ограничения
В этой задаче важен именно шаг `Копия` из урока `Списки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Последний элемент (practice)

**Коротко:** исправь выход за границу списка.

### Условие
В стартовом коде индекс слишком большой. Выведи последний элемент списка безопасным коротким способом.

### Вход
Нет входных данных.

### Выход
Одна строка.

### Пример
Вывод:
```text
web
```

### Что проверить
Для последнего элемента можно использовать индекс `-1`.

### Ограничения
В этой задаче важен именно шаг `Последний элемент` из урока `Списки`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 010. Список покупок (project)

**Коротко:** собери программу для строки покупок.

### Условие
Создай `main.py`. Программа читает строку с покупками через пробел, превращает её в список слов, добавляет `молоко`, затем выводит количество покупок и первую покупку.

Создай `README.md` с примером: ввод `хлеб сыр`, вывод `3` и `хлеб`.

### Вход
Одна строка со словами.

### Выход
Две строки: количество покупок после добавления и первая покупка.

### Пример
Ввод:
```text
хлеб сыр
```
Вывод:
```text
3
хлеб
```

### Что проверить
Сначала сделай список через `split()`, потом добавь новый элемент через `append()`.

### 011. Короткая проверка (summary)

Список хранит несколько значений в порядке. Индексы начинаются с `0`, последний элемент можно взять через `-1`. `append()` добавляет элемент, `len()` считает длину, `copy()` создаёт отдельный список.

Если появляется ошибка индекса, сравни индекс с длиной списка. Если исходный список неожиданно изменился, проверь, была ли сделана копия.

## 009. Функции

Estimated hours: 2.8
Topics: Python Core

### 001. Что это (theory)

### Что это
Функция — именованный блок кода. Она получает параметры, выполняет расчёт и возвращает ответ через `return`.

### Где это встречается в backend
В backend функции считают цены, проверяют входные данные и отделяют бизнес-правило от интерфейса.

### Минимальный пример
```python
def add_bonus(score):
    return score + 10

final_score = add_bonus(90)
print(final_score)
```

### Разбор по строкам
`def add_bonus(score):` объявляет функцию и параметр. `return` отдаёт значение наружу. `add_bonus(90)` вызывает функцию. `print` показывает готовый ответ.

### Что здесь важно
Важно отличать `return` от вывода на экран: возвращённое значение можно передать дальше.

### Частая ошибка
Частая ошибка: забыть `return` и получить `None`.

### Самопроверка
Что вернёт `add_bonus(90)`?

Проверь пример в редакторе, измени одно значение и сравни поведение. Если ответ изменился не так, как ожидалось, вернись к строке, где появляется термин темы `Функции`.

### 002. Минимальный пример (theory)

Разберём функцию:

```python
def greet(name):
    return "Привет, " + name

message = greet("Анна")
print(message)
```

Строка `def greet(name):` создаёт функцию с параметром `name`. Когда вызывается `greet("Анна")`, внутри функции `name` получает значение `"Анна"`. Строка `return "Привет, " + name` возвращает готовую строку. После этого результат попадает в переменную `message`.

Параметров может быть несколько:

```python
def area(width, height):
    return width * height
```

Функция получает ширину и высоту, возвращает произведение. Она не читает ввод сама и не выводит текст сама. Такую функцию проще проверять.

Параметр может иметь значение по умолчанию:

```python
def repeat(word, times=2):
    return word * times
```

Если второй аргумент не передать, используется `2`.

Частая ошибка: забыть вызвать функцию. Одного объявления `def` недостаточно: код внутри выполнится только при вызове.

Самопроверка: что вернёт `area(3, 4)`?

### 003. Функции (test)

Проверь понимание перед практикой по теме `Функции`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что делает `return` внутри функции?
- Q: Когда выполняется код внутри функции?
- Q: Почему функция для проверки обычно возвращает значение, а не печатает его?

### 004. Приветствие (practice)

**Коротко:** напиши функцию, которая возвращает приветствие.

### Условие
Реализуй функцию `greet(name)`. Она получает имя строкой и возвращает строку `Привет, <имя>`.

### Вход
Функция получает один параметр `name`.

### Выход
Функция возвращает строку.

### Пример
```python
greet("Анна")
```
Результат:
```text
Привет, Анна
```

### Что проверить
Нужен `return`, а не `print`. Проверка вызывает функцию и сравнивает возвращённое значение.

### Ограничения
В этой задаче важен именно шаг `Приветствие` из урока `Функции`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Площадь (practice)

**Коротко:** верни произведение двух параметров.

### Условие
Реализуй функцию `area(width, height)`. Она возвращает площадь прямоугольника.

### Вход
Два числа: ширина и высота.

### Выход
Число — произведение ширины и высоты.

### Пример
```python
area(3, 4)
```
Результат:
```text
12
```

### Что проверить
Функция не читает ввод. Она работает только с параметрами, которые ей передали.

### Ограничения
В этой задаче важен именно шаг `Площадь` из урока `Функции`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Чётное (practice)

**Коротко:** верни результат проверки числа.

### Условие
Реализуй функцию `is_even(number)`. Она возвращает `True`, если число чётное, и `False` в остальных случаях.

### Вход
Один целый параметр.

### Выход
Булево значение.

### Пример
```python
is_even(4)
```
Результат:
```text
True
```

### Что проверить
Остаток от деления на 2 берётся через `%`. У чётного числа остаток равен 0.

### Ограничения
В этой задаче важен именно шаг `Чётное` из урока `Функции`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. По умолчанию (practice)

**Коротко:** используй параметр со значением по умолчанию.

### Условие
Реализуй функцию `repeat_word(word, times=2)`. Она возвращает слово, повторённое `times` раз без пробелов.

### Вход
Строка `word` и необязательное число `times`.

### Выход
Строка.

### Пример
```python
repeat_word("ha", 3)
```
Результат:
```text
hahaha
```

### Что проверить
Если второй аргумент не передан, функция должна повторить слово два раза.

### Ограничения
В этой задаче важен именно шаг `По умолчанию` из урока `Функции`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Печать вместо return (practice)

**Коротко:** исправь функцию, которая печатает вместо возврата.

### Условие
В стартовом коде `add(a, b)` печатает сумму, но не возвращает её. Исправь функцию так, чтобы она возвращала сумму.

### Вход
Два числа через параметры функции.

### Выход
Возвращаемое число.

### Пример
```python
add(2, 3)
```
Результат:
```text
5
```

### Что проверить
Проверка не смотрит на экранный вывод. Ей нужен результат функции.

### Ограничения
В этой задаче важен именно шаг `Печать вместо return` из урока `Функции`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Чистая цена (practice)

**Коротко:** вынеси расчёт в функцию.

### Условие
Реализуй функцию `final_price(price, percent)`. Она возвращает цену после скидки `percent`. Используй формулу `price - price * percent / 100`.

### Вход
Два числа через параметры.

### Выход
Число.

### Пример
```python
final_price(1000, 10)
```
Результат:
```text
900.0
```

### Что проверить
Функция не должна читать ввод и не должна печатать результат. Она только возвращает число.

### Ограничения
В этой задаче важен именно шаг `Чистая цена` из урока `Функции`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 010. Калькулятор скидки (project)

**Коротко:** собери модуль с функцией расчёта скидки.

### Условие
Создай `main.py`. В нём должна быть функция `final_price(price, percent)`, которая возвращает цену после скидки. Ниже в файле прочитай цену и процент, вызови функцию и выведи результат.

Создай `README.md` с примером: ввод `1000` и `10`, вывод `900.0`.

### Вход
Две строки: цена и процент скидки.

### Выход
Одно число.

### Пример
Ввод:
```text
1000
10
```
Вывод:
```text
900.0
```

### Что проверить
Расчёт должен быть внутри функции, а ввод и вывод — ниже после объявления функции.

### 011. Короткая проверка (summary)

Функция объявляется через `def`, получает параметры и возвращает результат через `return`. Код внутри функции выполняется только при вызове. Для проверяемых задач функция обычно не читает ввод и не печатает сама.

Если проверка говорит, что результат равен `None`, почти всегда забыто `return`.

## 010. Кортежи

Estimated hours: 2.8
Topics: Python Core

### 001. Что это (theory)

Кортеж похож на список, но обычно используется для небольшого набора связанных значений, который не нужно менять. Координата точки, пара чисел, результат из двух значений — хорошие примеры для кортежа.

Кортеж записывают в круглых скобках: `point = (10, 20)`. Элементы тоже доступны по индексам: `point[0]` берёт первое значение, `point[1]` — второе. Кортеж можно распаковать: `x, y = point`.

Главное отличие от списка: кортеж не меняют по месту. Если нужна новая координата, создают новый кортеж. Это делает такие данные более предсказуемыми: случайный код не изменит точку внутри программы.

Функция может вернуть кортеж. Так удобно вернуть сразу два значения без создания отдельного объекта.

Что важно: кортеж с одним элементом пишется с запятой: `(value,)`. Без запятой это просто значение в скобках.

Частая ошибка: попытаться изменить элемент кортежа через индекс. Python не разрешит это, потому что кортеж неизменяемый.

Самопроверка: что хранит `point = (3, 5)`?

Минимальный пример для этой части:

```python
# Кортежи: концептуальный разбор
# термин -> граница -> проверка
point = (3, 5)
x, y = point
print(x + y)
```

Разбор: кортеж хранит два значения, распаковка кладёт их в переменные `x` и `y`.

### 002. Минимальный пример (theory)

Пример координаты:

```python
point = (3, 5)
print(point[0])
print(point[1])
```

Первый вывод — `3`, второй — `5`. Индексы работают так же, как у строк и списков: первый индекс равен `0`.

Пример распаковки:

```python
point = (3, 5)
x, y = point
print(x + y)
```

Python берёт первое значение кортежа в `x`, второе — в `y`. Вывод будет `8`.

Пример функции:

```python
def move(point, dx, dy):
    x, y = point
    return (x + dx, y + dy)
```

Функция получает точку и сдвиг, распаковывает координаты и возвращает новый кортеж. Исходная точка не меняется.

Частая ошибка: писать `point[0] = 10`. Для кортежа так нельзя. Создай новый кортеж с нужными значениями.

Самопроверка: зачем в функции `move` возвращается новый кортеж?

Разбор примера для темы «Кортежи»: сначала найди в примере входные данные, потом строку, где появляется новый инструмент, и только после этого смотри на результат. Если код не запускается, читай первое сообщение об ошибке сверху: там обычно видно строку и причину. Не переписывай пример вслепую. Измени одно значение, запусти снова и проверь, как меняется поведение. Так ты связываешь правило с наблюдаемым результатом, а не запоминаешь формулировку отдельно от кода.

### 003. Кортежи (test)

Проверь понимание перед практикой по теме `Кортежи`. Вопросы проверяют конкретное поведение, границу контракта и типичную ошибку этого урока.

- Q: Что вернёт `(10, 20)[0]`?
- Q: Что делает строка `x, y = point`?
- Q: Почему нельзя писать `point[0] = 10` для кортежа?

### 004. Точка (practice)

**Коротко:** верни координату как кортеж.

### Условие
Реализуй функцию `make_point(x, y)`. Она возвращает кортеж из двух значений: `x` и `y`.

### Вход
Два параметра.

### Выход
Кортеж из двух элементов.

### Пример
```python
make_point(3, 5)
```
Результат:
```text
(3, 5)
```

### Что проверить
Нужен именно кортеж, а не строка и не список.

### Ограничения
В этой задаче важен именно шаг `Точка` из урока `Кортежи`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 005. Координата x (practice)

**Коротко:** возьми первое значение из точки.

### Условие
Реализуй функцию `get_x(point)`. Она получает кортеж из двух значений и возвращает первое значение.

### Вход
Кортеж `point`.

### Выход
Первый элемент кортежа.

### Пример
```python
get_x((8, 4))
```
Результат:
```text
8
```

### Что проверить
Первый индекс равен `0`.

### Ограничения
В этой задаче важен именно шаг `Координата x` из урока `Кортежи`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 006. Распаковка (practice)

**Коротко:** распакуй кортеж и сложи координаты.

### Условие
Реализуй функцию `coordinate_sum(point)`. Распакуй кортеж в `x` и `y`, затем верни сумму.

### Вход
Кортеж из двух чисел.

### Выход
Число.

### Пример
```python
coordinate_sum((3, 5))
```
Результат:
```text
8
```

### Что проверить
Используй распаковку `x, y = point`, чтобы код явно показывал две координаты.

### Ограничения
В этой задаче важен именно шаг `Распаковка` из урока `Кортежи`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 007. Сдвиг (practice)

**Коротко:** верни новую точку после сдвига.

### Условие
Реализуй функцию `move(point, dx, dy)`. Она получает точку `(x, y)` и возвращает новую точку `(x + dx, y + dy)`.

### Вход
Кортеж точки и два числа сдвига.

### Выход
Новый кортеж.

### Пример
```python
move((1, 2), 10, 20)
```
Результат:
```text
(11, 22)
```

### Что проверить
Исходный кортеж менять нельзя. Нужно вернуть новый кортеж.

### Ограничения
В этой задаче важен именно шаг `Сдвиг` из урока `Кортежи`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 008. Новый кортеж (practice)

**Коротко:** исправь попытку изменить кортеж.

### Условие
В стартовом коде функция пытается изменить первый элемент кортежа. Так нельзя. Исправь `replace_x(point, new_x)`, чтобы она возвращала новый кортеж с новым `x` и старым `y`.

### Вход
Кортеж точки и новое значение `x`.

### Выход
Новый кортеж.

### Пример
```python
replace_x((3, 5), 10)
```
Результат:
```text
(10, 5)
```

### Что проверить
Не присваивай в `point[0]`. Сначала возьми старый `y`, потом верни новый кортеж.

### Ограничения
В этой задаче важен именно шаг `Новый кортеж` из урока `Кортежи`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 009. Минимум и максимум (practice)

**Коротко:** верни пару значений.

### Условие
Реализуй функцию `min_max(a, b)`. Она возвращает кортеж: сначала меньшее число, потом большее.

### Вход
Два числа.

### Выход
Кортеж из двух чисел.

### Пример
```python
min_max(9, 4)
```
Результат:
```text
(4, 9)
```

### Что проверить
Используй условие для выбора порядка. При равных числах верни пару из этих же чисел.

### Ограничения
В этой задаче важен именно шаг `Минимум и максимум` из урока `Кортежи`. Не добавляй поясняющие слова в вывод. Если пример показывает одно число или одну строку, выведи только это значение.

### Самопроверка перед отправкой
Сначала запусти пример из условия. Затем проверь крайний случай, который описан в разделе “Что проверить”. Если код не запускается, сначала прочитай последнюю строку ошибки и найди строку, на которую она указывает.

### 010. Координата точки (project)

**Коротко:** собери модуль для работы с координатой.

### Условие
Создай `main.py`. В файле должны быть функции `move(point, dx, dy)` и `format_point(point)`.

`move` возвращает новую точку после сдвига. `format_point` возвращает строку вида `(x, y)`. Ниже в файле прочитай четыре числа: `x`, `y`, `dx`, `dy`; создай точку, сдвинь её и выведи форматированную строку.

Создай `README.md` с примером: ввод `1`, `2`, `10`, `20`, вывод `(11, 22)`.

### Вход
Четыре целых числа, каждое на своей строке.

### Выход
Одна строка с координатой.

### Пример
Ввод:
```text
1
2
10
20
```
Вывод:
```text
(11, 22)
```

### Что проверить
Координата хранится кортежем. Сдвиг возвращает новый кортеж, а не меняет старый.

### 011. Короткая проверка (summary)

Кортеж хранит несколько связанных значений и не меняется по месту. Индексы и распаковка помогают читать элементы. Если нужно “изменить” кортеж, создают новый кортеж.

Если видишь ошибку при присваивании в `point[0]`, причина в неизменяемости кортежа.

## 011. Множества

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это Множество хранит уникальные элементы и быстро проверяет наличие значения. ### Зачем это нужно Порядок set не является контрактом; сравнивай содержимое, а не строковый вывод. ### Где это встречается в backend В backend set используют для дедупликации id, проверки прав и пересечения тегов. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Множества». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Множества» проверь три вещи: какой вход рассматривается, какой результат получае
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 012. Словари

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это Словарь связывает key и value. По key можно быстро получить значение. ### Зачем это нужно `[]` подходит для обязательного key, `get()` — для необязательного. ### Где это встречается в backend В backend dict лежит в основе JSON-ответов, конфигурации, подсчётов и группировки. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Словари». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Словари» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 013. map/filter/lambda

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это `map` преобразует элементы, `filter` оставляет подходящие, `lambda` задаёт маленькую функцию прямо на месте. ### Зачем это нужно Сложную логику лучше вынести в обычную функцию, а не прятать в lambda. ### Где это встречается в backend В backend это встречается при нормализации id и фильтрации небольших списков перед ответом API. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «map/filter/lambda». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «map/filter/lambda» проверь три
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 014. datetime

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это `datetime` хранит дату и время как объект, а `timedelta` описывает сдвиг во времени. ### Зачем это нужно Договорись о формате и timezone; на сервере часто используют UTC. ### Где это встречается в backend В backend даты нужны для дедлайнов, токенов, логов, подписок и фильтров по периоду. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «datetime». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «datetime» проверь три вещи: какой вход рассматривается, какой результат получаетс
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 015. Итераторы

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это Итератор отдаёт значения по одному и помнит текущую позицию. ### Зачем это нужно Итератор расходуется; повторный проход не начнётся с начала сам. ### Где это встречается в backend В backend итераторы помогают читать большие файлы, SQL-выборки и потоки событий без загрузки всего в память. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Итераторы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Итераторы» проверь три вещи: какой вход рассматривается, какой результат получае
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 016. Генераторы

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это Генератор создаёт значения лениво через `yield`, не строя весь список сразу. ### Зачем это нужно Код генератора выполняется только при обходе. ### Где это встречается в backend В backend генераторы полезны для чтения файлов, экспорта строк и обработки больших наборов. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Генераторы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Генераторы» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 017. Декораторы

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это Декоратор оборачивает функцию и добавляет поведение вокруг вызова. ### Зачем это нужно Обёртка должна вернуть ответ исходной функции. ### Где это встречается в backend В backend декораторы встречаются в маршрутах, retry, логировании и проверке прав. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Декораторы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Декораторы» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убр
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 018. Контекстные менеджеры

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это `with` управляет ресурсом: открыть, передать в работу и закрыть даже при ошибке. ### Зачем это нужно Не используй ресурс после выхода из `with`. ### Где это встречается в backend В backend это файлы, транзакции, временные директории и lock-и. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Контекстные менеджеры». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Контекстные менеджеры» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка поя
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 019. Исключения

Estimated hours: 3.6
Topics: Python Core

Goal: ### Что это Исключение сообщает, что операция не смогла завершиться нормальным способом. ### Зачем это нужно Лови конкретные исключения, чтобы не скрывать настоящие сбои. ### Где это встречается в backend В backend исключения появляются при неверном JSON, отсутствии записи, ошибке сети и сбое внешнего сервиса. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Исключения». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Исключения» проверь три вещи: какой вход рассматривается, какой резу
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 020. AI для учёбы

Estimated hours: 3.7
Topics: AI для учёбы

Goal: ### Что это AI API получает messages: system задаёт правила, user задаёт запрос. В проверках используется mock provider. ### Зачем это нужно Секреты нельзя отправлять в prompt; structured output нужно валидировать schema. ### Где это встречается в backend AI-интеграция должна иметь timeout, retry, rate limit, structured output schema и no secrets. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «AI для учёбы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «AI для учёбы» проверь три ве
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 021. Как работает компьютер

Estimated hours: 4.2
Topics: среда и терминал

Goal: ### Что это Python-программа — текстовый файл, который интерпретатор читает и выполняет сверху вниз. ### Зачем это нужно Файл с кодом и данные в памяти — разные вещи. ### Где это встречается в backend Backend-сервис — process в операционной системе: он занимает память, слушает port и читает конфигурацию. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Как работает компьютер». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Как работает компьютер» проверь три вещи: какой вход рассматри
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 022. Терминал: базовые команды

Estimated hours: 4.2
Topics: среда и терминал

Goal: ### Что это Терминал запускает команды операционной системы и показывает их вывод. ### Зачем это нужно Всегда проверяй текущую папку перед запуском команды. ### Где это встречается в backend Backend-разработчик использует терминал для Python, Git, Docker, миграций и серверных логов. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Терминал: базовые команды». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Терминал: базовые команды» проверь три вещи: какой вход рассматривается, какой ре
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 023. Терминал: процессы и bash

Estimated hours: 4.2
Topics: среда и терминал

Goal: ### Что это Процесс — запущенная программа. Bash помогает запускать процессы, передавать env и останавливать их. ### Зачем это нужно Команда запускает процесс, но процесс живёт отдельно и имеет pid. ### Где это встречается в backend Backend-сервер — долгий процесс, который слушает порт и пишет logs. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Терминал: процессы и bash». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Терминал: процессы и bash» проверь три вещи: какой вход рассматр
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 024. Git: первые коммиты

Estimated hours: 4.2
Topics: Git и GitHub

Goal: ### Что это Git хранит историю проекта. Первый цикл: `git init`, `status`, `add`, `commit`. ### Зачем это нужно Commit сохраняет только staged files. ### Где это встречается в backend В backend Git нужен для review, отката ошибок и выпуска релизов. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Git: первые коммиты». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Git: первые коммиты» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, есл
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 025. Git: индекс и история

Estimated hours: 4.2
Topics: Git и GitHub

Goal: ### Что это Git index — промежуточная зона между рабочими файлами и commit. ### Зачем это нужно Проверяй staged diff перед commit. ### Где это встречается в backend Индекс помогает разделять изменения: API отдельно, migration отдельно. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Git: индекс и история». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Git: индекс и история» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 026. Git: ветки

Estimated hours: 4.2
Topics: Git и GitHub

Goal: ### Что это Branch — указатель на линию разработки. Он отделяет feature от main. ### Зачем это нужно Перед изменениями проверь текущую branch. ### Где это встречается в backend Ветки используют для feature, hotfix и pull request. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Git: ветки». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Git: ветки» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбо
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 027. Git: отмена изменений

Estimated hours: 4.2
Topics: Git и GitHub

Goal: ### Что это В Git есть разные отмены: восстановить файл, убрать из index, создать обратный commit. ### Зачем это нужно Для общей ветки безопаснее `revert`. ### Где это встречается в backend Это нужно при случайном debug, ошибочном релизе или неверном файле. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Git: отмена изменений». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Git: отмена изменений» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 028. Git: merge и rebase

Estimated hours: 4.2
Topics: Git и GitHub

Goal: ### Что это merge объединяет ветки, rebase переносит commits поверх новой базы. ### Зачем это нужно Rebase переписывает локальную историю. ### Где это встречается в backend Перед review backend-разработчик синхронизирует feature с main. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Git: merge и rebase». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Git: merge и rebase» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать клю
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 029. GitHub и remote

Estimated hours: 4.2
Topics: Git и GitHub

Goal: ### Что это Remote — удалённый repo. GitHub хранит ветки и запускает review/CI. ### Зачем это нужно Non-fast-forward означает, что remote ушёл вперёд. ### Где это встречается в backend Через remote команда делает pull request, CI и релизы. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «GitHub и remote». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «GitHub и remote» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 030. CLI-проект: менеджер задач

Estimated hours: 4.2
Topics: CLI-проект, среда и терминал, Git и GitHub

Goal: ### Что это CLI-программа получает arguments из командной строки и печатает ответ в stdout. ### Зачем это нужно Отделяй parsing от бизнес-логики. ### Где это встречается в backend В backend CLI используют для миграций, импорта, обслуживания очередей и локальных утилит. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «CLI-проект: менеджер задач». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «CLI-проект: менеджер задач» проверь три вещи: какой вход рассматривается, какой результат полу
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 031. Экзамен 1

Estimated hours: 4.2
Topics: Python Core

Goal: ### Что это Экзамен собирает уже изученные навыки в одну задачу с понятным контрактом. ### Зачем это нужно Перед кодом выпиши вход, выход и ошибки. ### Где это встречается в backend В backend так выглядят реальные задачи: прочитать требования, выделить edge cases, написать код и проверить. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Экзамен 1». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Экзамен 1» проверь три вещи: какой вход рассматривается, какой результат получается и кака
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

# Модуль 2. Python Hard

## 001. ООП: классы

Estimated hours: 4.0
Topics: ООП

Goal: ### Класс и объект Класс описывает, какие данные хранит объект и какие действия он умеет выполнять. Объект — это конкретный экземпляр класса: например, одна задача, один пользователь или один счёт. Без класса связанные данные быстро расползаются по разным словарям и функциям. Класс держит состояние рядом с действиями, которые это состояние меняют. Минимальный пример: Строка `class Task` создаёт новый тип объектов. `__init__` запускается при создании `Task(...)`. `self.title` и `self.done` — состояние конкретного объекта. Метод `complete` меняет состояние именно этого объекта. Где это встречается в backend: модель пользователя, заказ, платёж,
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 002. ООП: атрибуты

Estimated hours: 4.2
Topics: ООП

Goal: ### Что это Атрибуты объекта хранят его состояние. ### Зачем это нужно Атрибут живёт после выхода из метода, локальная переменная — нет. ### Где это встречается в backend В backend object может хранить status, balance, настройки или ошибки validation. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «ООП: атрибуты». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «ООП: атрибуты» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 003. ООП: методы

Estimated hours: 4.2
Topics: ООП

Goal: ### Что это Метод — функция класса, которая получает объект через `self` и работает с его состоянием. ### Зачем это нужно Изменение состояния лучше держать в методе с понятным именем. ### Где это встречается в backend Методы доменных объектов закрывают задачи, списывают деньги и меняют status. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «ООП: методы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «ООП: методы» проверь три вещи: какой вход рассматривается, какой результат получаетс
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 004. Инкапсуляция

Estimated hours: 4.2
Topics: ООП

Goal: ### Что это Инкапсуляция защищает invariant объекта и не даёт записать недопустимое состояние. ### Зачем это нужно Состояние меняется только через метод, который защищает invariant. ### Где это встречается в backend Баланс, статус заказа и email должны меняться через методы с validation. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Инкапсуляция». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Инкапсуляция» проверь три вещи: какой вход рассматривается, какой результат получается и
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 005. Композиция

Estimated hours: 4.2
Topics: ООП

Goal: ### Что это Композиция собирает объект из других объектов и делегирует им часть поведения. ### Зачем это нужно Передавай зависимость снаружи, чтобы её можно было подменить. ### Где это встречается в backend Service использует repository, mailer, cache adapter или payment provider. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Композиция». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Композиция» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибк
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 006. Магические методы

Estimated hours: 4.2
Topics: магические методы, ООП

Goal: ### Что это Magic methods задают поведение объекта для `==`, `len`, `in`, `str` и других операций. ### Зачем это нужно Dunder method должен отражать реальное поведение объекта. ### Где это встречается в backend Value objects вроде Money или PermissionSet становятся понятнее через dunder methods. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Магические методы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Магические методы» проверь три вещи: какой вход рассматривается, какой резул
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 007. Протоколы Python

Estimated hours: 4.2
Topics: протоколы

Goal: ### Что это Protocol описывает структурный interface: подходит любой объект с нужными методами. ### Зачем это нужно Protocol описывает поведение, а не конкретный класс. ### Где это встречается в backend Protocol отделяет service от конкретного provider или repository. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Протоколы Python». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Протоколы Python» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 008. Наследование

Estimated hours: 4.2
Topics: наследование и полиморфизм, ООП

Goal: ### Что это Наследование создаёт subclass на основе base class и позволяет override method. ### Зачем это нужно Используй inheritance только для отношения «является разновидностью». ### Где это встречается в backend Уместно для ошибок, небольших стратегий и общих базовых моделей. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Наследование». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Наследование» проверь три вещи: какой вход рассматривается, какой результат получается и какая ош
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 009. Полиморфизм

Estimated hours: 4.2
Topics: наследование и полиморфизм, ООП

Goal: ### Что это Полиморфизм позволяет вызывать один метод у разных объектов без `isinstance`. ### Зачем это нужно Добавление нового sender не требует менять `notify`. ### Где это встречается в backend Так подключают provider, sender, serializer, storage adapter и strategy. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Полиморфизм». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Полиморфизм» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 010. Абстракции

Estimated hours: 4.2
Topics: наследование и полиморфизм, ООП

Goal: ### Что это Абстракция задаёт interface и boundary, скрывая implementation detail за adapter или repository. ### Зачем это нужно Скрывай детали там, где они мешают проверять service. ### Где это встречается в backend Service работает с repository или gateway, не зная SQL или HTTP SDK. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Абстракции». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Абстракции» проверь три вещи: какой вход рассматривается, какой результат получается и какая о
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 011. dataclasses

Estimated hours: 4.2
Topics: ООП, типизация

Goal: ### Что это `@dataclass` создаёт `__init__`, repr и сравнение для класса данных. ### Зачем это нужно Для изменяемых defaults используй factory. ### Где это встречается в backend DTO, command objects и value objects часто удобно описывать dataclass. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «dataclasses». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «dataclasses» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключеву
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 012. typing: база

Estimated hours: 4.2
Topics: типизация

Goal: ### Что это Type hints описывают типы параметров и ответа функции. ### Зачем это нужно Аннотация не заменяет validation внешнего ввода. ### Где это встречается в backend В backend аннотации помогают ловить ошибки DTO, id и nullable значений до запуска. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «typing: база». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «typing: база» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать к
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 013. typing: Optional и Union

Estimated hours: 4.2
Topics: типизация

Goal: ### Что это `Optional` означает значение или `None`, а `Union` — несколько допустимых типов. ### Зачем это нужно Перед строковыми методами проверь `is None`. ### Где это встречается в backend В backend Optional появляется при поиске записи и необязательных полях. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «typing: Optional и Union». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «typing: Optional и Union» проверь три вещи: какой вход рассматривается, какой результат получается и к
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 014. typing: generics

Estimated hours: 4.2
Topics: типизация

Goal: ### Что это Generic-типы сохраняют тип элемента внутри контейнера: `list[str]`, `Page[T]`. ### Зачем это нужно Не теряй тип элемента внутри контейнера. ### Где это встречается в backend Generics полезны для response wrappers, pagination и repository. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «typing: generics». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «typing: generics» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если уб
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 015. mypy и контракты

Estimated hours: 4.0
Topics: типизация, pytest

Goal: ### Типы как контракт Аннотация типа показывает, какие значения функция принимает и что возвращает. mypy читает эти аннотации до запуска программы и находит часть ошибок раньше, чем пользователь увидит сбой. В больших backend-проектах один объект проходит через API, сервис, репозиторий и тесты. Типы помогают не перепутать `str`, `int`, `None` и структуру словаря. Минимальный пример: `hours: int` и `minutes: int` описывают вход. `-> int` описывает результат. Строка `value: int` фиксирует ожидание вызывающего кода. Где это встречается в backend: контракты DTO, ответы сервисов, параметры репозиториев и функции расчёта часто аннотируются, чтобы р
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 016. Файлы и pathlib

Estimated hours: 4.0
Topics: Python Core

Goal: ### Path вместо строк `pathlib.Path` представляет путь как объект. У него есть методы для проверки существования, создания директорий, чтения и записи файлов. Строки путей легко склеить неправильно. `Path` знает разделители ОС и делает код чтения файлов понятнее. Минимальный пример: `Path('data')` создаёт путь к директории. Оператор `/` добавляет часть пути. `exists()` проверяет, есть ли файл или директория. Где это встречается в backend: конфиги, загруженные CSV, временные файлы задач и артефакты деплоя читаются через пути. Частая ошибка: склеить путь через `+ '/' +`, а потом получить неверный разделитель или двойной слеш. Самопроверка: кака
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 017. JSON и CSV

Estimated hours: 4.0
Topics: Python Core

Goal: ### Что это JSON хранит вложенные данные как строки, числа, списки и словари. CSV хранит таблицу строками и колонками. Backend часто принимает JSON из API и выгружает таблицы в CSV для аналитики или ручной проверки. Минимальный пример: Разбери пример: импорт подключает модуль, входные данные превращаются в структуру текущей темы, затем результат становится проверяемым. Где это встречается в backend: request body, webhook payload, экспорт заказов и отчёты для поддержки. Частая ошибка: подменить тему урока универсальной функцией без нужного инструмента. Самопроверка: какая строка использует главный инструмент урока? Разбор примера для темы «JSO
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 018. Логирование

Estimated hours: 4.0
Topics: Python Core

Goal: ### Что это Логирование записывает важные события программы: что произошло, с каким уровнем важности и с каким контекстом. В backend логи помогают понять, почему endpoint вернул ошибку, почему фоновой задаче не хватило данных и где случился сбой. Минимальный пример: Разбери пример: импорт подключает модуль, входные данные превращаются в структуру текущей темы, затем результат становится проверяемым. Где это встречается в backend: ошибки API, фоновые задачи, платежи, интеграции и деплойные проверки. Частая ошибка: подменить тему урока универсальной функцией без нужного инструмента. Самопроверка: какая строка использует главный инструмент урока
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 019. Пакеты и импорты

Estimated hours: 4.0
Topics: Python Core

Goal: ### Что это Импорт подключает код из другого модуля. Пакет группирует несколько модулей в директории с `__init__.py`. В backend проекте роутеры, сервисы, модели и репозитории лежат в разных файлах. Импорты связывают эти части без копирования кода. Минимальный пример: Разбери пример: импорт подключает модуль, входные данные превращаются в структуру текущей темы, затем результат становится проверяемым. Где это встречается в backend: разделение FastAPI routers, service layer, database repositories и утилит. Частая ошибка: подменить тему урока универсальной функцией без нужного инструмента. Самопроверка: какая строка использует главный инструмент
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 020. Poetry

Estimated hours: 4.2
Topics: Poetry

Goal: ### Что это Poetry управляет dependencies, virtualenv, `pyproject.toml` и lock-файлом. ### Зачем это нужно Lock-файл нужно коммитить для воспроизводимости. ### Где это встречается в backend В backend Poetry даёт одинаковое окружение локально, в CI и на сервере. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Poetry». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Poetry» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключ
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 021. pytest: функции

Estimated hours: 4.0
Topics: pytest

Goal: ### Что это В pytest проверка — это функция, имя которой начинается с `test_`. Внутри используется `assert`: если выражение истинно, проверка проходит; если ложно, pytest показывает место сбоя. ### Зачем это нужно Проверка должна фиксировать правило, а не просто запускать код. Один `assert` должен отвечать на конкретный вопрос о поведении функции. ### Где это встречается в backend Так проверяют чистые функции сервиса: расчёт цены, нормализацию email, выбор статуса, проверку прав. Маленькая проверка быстрее показывает ошибку, чем ручной запуск всего API. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 022. pytest: fixtures

Estimated hours: 2.5
Topics: pytest

Goal: ### Что это Тема «pytest: fixtures» вводит отдельный навык, который дальше используется в задачах курса. ### Зачем это нужно Отделяй новое понятие от имён переменных: имя можно заменить, а смысл операции должен остаться тем же. Контекст этого урока: «pytest: fixtures». ### Где это встречается в backend В backend-разработке «pytest: fixtures» используется там, где нужно получить устойчивое поведение кода и понятный результат. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «pytest: fixtures». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, за
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 023. pytest: parametrization

Estimated hours: 2.5
Topics: pytest

Goal: ### Что это Тема «pytest: parametrization» вводит отдельный навык, который дальше используется в задачах курса. ### Зачем это нужно Отделяй новое понятие от имён переменных: имя можно заменить, а смысл операции должен остаться тем же. Контекст этого урока: «pytest: parametrization». ### Где это встречается в backend В backend-разработке «pytest: parametrization» используется там, где нужно получить устойчивое поведение кода и понятный результат. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «pytest: parametrization». Сначала найди термин урока, затем строку, где он вли
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 024. pytest: mocking

Estimated hours: 2.5
Topics: pytest

Goal: ### Что это Тема «pytest: mocking» вводит отдельный навык, который дальше используется в задачах курса. ### Зачем это нужно Отделяй новое понятие от имён переменных: имя можно заменить, а смысл операции должен остаться тем же. Контекст этого урока: «pytest: mocking». ### Где это встречается в backend В backend-разработке «pytest: mocking» используется там, где нужно получить устойчивое поведение кода и понятный результат. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «pytest: mocking». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 025. pytest: coverage

Estimated hours: 2.5
Topics: pytest

Goal: ### Что это Тема «pytest: coverage» вводит отдельный навык, который дальше используется в задачах курса. ### Зачем это нужно Отделяй новое понятие от имён переменных: имя можно заменить, а смысл операции должен остаться тем же. Контекст этого урока: «pytest: coverage». ### Где это встречается в backend В backend-разработке «pytest: coverage» используется там, где нужно получить устойчивое поведение кода и понятный результат. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «pytest: coverage». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, за
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 026. Алгоритмы: сложность

Estimated hours: 4.2
Topics: алгоритмы и структуры данных

Goal: ### Что это Сложность показывает, как растёт operation count: O(n), O(log n), O(n^2). ### Зачем это нужно Смотри на рост операций, а не на длину кода. ### Где это встречается в backend В backend это влияет на импорт данных и выбор структуры. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Алгоритмы: сложность». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Алгоритмы: сложность» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убр
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 027. Массивы и списки

Estimated hours: 4.2
Topics: алгоритмы и структуры данных

Goal: ### Что это Список хранит элементы по индексам и поддерживает slices, append, insert и mutation. ### Зачем это нужно Срез не меняет исходный list. ### Где это встречается в backend В backend list приходит в JSON arrays и batch-операциях. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Массивы и списки». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Массивы и списки» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 028. Hash map и set

Estimated hours: 4.2
Topics: алгоритмы и структуры данных

Goal: ### Что это dict и set используют hash для быстрого membership и группировки. ### Зачем это нужно Key должен быть hashable. ### Где это встречается в backend В backend они нужны для frequency map, unique ids и grouping. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Hash map и set». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Hash map и set» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 029. Stack, queue, deque

Estimated hours: 4.2
Topics: алгоритмы и структуры данных

Goal: ### Что это Stack работает как LIFO, queue как FIFO, deque быстро делает append/pop/popleft. ### Зачем это нужно Для stack используй `append` и `pop`. ### Где это встречается в backend В backend queue обрабатывает фоновые задачи, stack хранит undo, deque подходит для sliding window. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Stack, queue, deque». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Stack, queue, deque» проверь три вещи: какой вход рассматривается, какой результат полу
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 030. Сортировки

Estimated hours: 4.2
Topics: алгоритмы и структуры данных

Goal: ### Что это Сортировка упорядочивает элементы: `sorted` возвращает новый list, `.sort()` меняет исходный. ### Зачем это нужно `sort()` возвращает None. ### Где это встречается в backend В backend sorted нужен для top-N, stable order и API response. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Сортировки». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Сортировки» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 031. Binary search

Estimated hours: 4.2
Topics: алгоритмы и структуры данных

Goal: ### Что это Binary search делит sorted диапазон пополам и даёт O(log n). ### Зачем это нужно Данные должны быть sorted. ### Где это встречается в backend Идея лежит в индексах БД и поиске границ. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Binary search». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Binary search» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от следующего ш
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 032. threading

Estimated hours: 4.2
Topics: threading

Goal: ### Что это Thread — поток внутри process; он полезен для ожидания I/O, но не для CPU-bound ускорения Python. ### Зачем это нужно Общие данные между threads нужно защищать. ### Где это встречается в backend В backend thread встречается в сетевых ожиданиях и фоновых адаптерах. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «threading». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «threading» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появи
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 033. multiprocessing

Estimated hours: 4.2
Topics: multiprocessing

Goal: ### Что это multiprocessing запускает отдельный Process; у каждого своя память. Это подходит для CPU-bound. ### Зачем это нужно Передавай сериализуемые данные. ### Где это встречается в backend В backend Pool используют для тяжёлой обработки и worker-задач. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «multiprocessing». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «multiprocessing» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, ес
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 034. asyncio: база

Estimated hours: 4.2
Topics: asyncio

Goal: ### Что это asyncio переключает tasks в точках `await`, пока код ждёт I/O. ### Зачем это нужно Async не ускоряет CPU-bound цикл. ### Где это встречается в backend В backend async нужен для HTTP clients, WebSocket и очередей. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «asyncio: база». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «asyncio: база» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разб
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 035. asyncio: задачи и очереди

Estimated hours: 4.2
Topics: asyncio

Goal: ### Что это asyncio переключает tasks в точках `await`, пока код ждёт I/O. ### Зачем это нужно Async не ускоряет CPU-bound цикл. ### Где это встречается в backend В backend async нужен для HTTP clients, WebSocket и очередей. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «asyncio: задачи и очереди». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «asyncio: задачи и очереди» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать клю
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 036. asyncio: HTTP

Estimated hours: 4.2
Topics: asyncio, HTTP / HTTPS

Goal: ### Что это asyncio переключает tasks в точках `await`, пока код ждёт I/O. ### Зачем это нужно Async не ускоряет CPU-bound цикл. ### Где это встречается в backend В backend async нужен для HTTP clients, WebSocket и очередей. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «asyncio: HTTP». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «asyncio: HTTP» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разб
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

# Модуль 3. SQL, базы данных и сети

## 001. SQL: SELECT

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это SELECT читает columns из table и возвращает rows. ### Зачем это нужно Не используй SELECT * без причины. ### Где это встречается в backend Любой endpoint чтения начинается с SELECT. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: SELECT». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: SELECT» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от следующего шага: з
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 002. SQL: WHERE

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это WHERE фильтрует rows; LIKE, IN, BETWEEN и NULL уточняют условия. ### Зачем это нужно NULL проверяется через IS NULL. ### Где это встречается в backend Фильтры защищают API от выдачи лишних данных. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: WHERE». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: WHERE» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от следу
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 003. SQL: ORDER BY и LIMIT

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это ORDER BY задаёт порядок, LIMIT/OFFSET выбирают страницу. ### Зачем это нужно Pagination без ORDER BY нестабильна. ### Где это встречается в backend Это основа pagination в API. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: ORDER BY и LIMIT». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: ORDER BY и LIMIT» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от сле
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 004. SQL: LIKE, IN, BETWEEN, NULL

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это WHERE фильтрует rows; LIKE, IN, BETWEEN и NULL уточняют условия. ### Зачем это нужно NULL проверяется через IS NULL. ### Где это встречается в backend Фильтры защищают API от выдачи лишних данных. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: LIKE, IN, BETWEEN, NULL». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: LIKE, IN, BETWEEN, NULL» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую стро
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 005. SQL: агрегаты

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это Агрегаты считают COUNT, SUM, AVG, MIN, MAX; GROUP BY делает расчёт по группам. ### Зачем это нужно Обычная column в SELECT должна быть в GROUP BY или агрегате. ### Где это встречается в backend Это основа статистики и reports. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: агрегаты». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: агрегаты» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую стро
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 006. SQL: GROUP BY

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это Агрегаты считают COUNT, SUM, AVG, MIN, MAX; GROUP BY делает расчёт по группам. ### Зачем это нужно Обычная column в SELECT должна быть в GROUP BY или агрегате. ### Где это встречается в backend Это основа статистики и reports. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: GROUP BY». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: GROUP BY» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую стро
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 007. SQL: HAVING

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это HAVING фильтрует groups после GROUP BY. ### Зачем это нужно WHERE работает до группировки, HAVING после. ### Где это встречается в backend Используется в reports по пользователям, заказам и revenue. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: HAVING». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: HAVING» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от с
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 008. SQL: INNER JOIN

Estimated hours: 3.5
Topics: SQL

Goal: ### Что это INNER JOIN соединяет tables по ключам через условие ON. ### Зачем это нужно Без ON получится лишнее перемножение rows. ### Где это встречается в backend JOIN нужен, чтобы API вернул данные ресурса вместе со связанными сущностями. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: INNER JOIN». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: INNER JOIN» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключев
Step mix: {'theory': 2, 'test': 2, 'practice': 3, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 3, 'ide_plugin': 2}

## 009. SQL: LEFT JOIN

Estimated hours: 3.5
Topics: SQL

Goal: ### Что это LEFT JOIN соединяет tables по ключам через условие ON. ### Зачем это нужно Без ON получится лишнее перемножение rows. ### Где это встречается в backend JOIN нужен, чтобы API вернул данные ресурса вместе со связанными сущностями. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: LEFT JOIN». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: LEFT JOIN» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую
Step mix: {'theory': 2, 'test': 2, 'practice': 3, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 3, 'ide_plugin': 2}

## 010. SQL: self join

Estimated hours: 3.5
Topics: SQL

Goal: ### Что это INNER JOIN соединяет tables по ключам через условие ON. ### Зачем это нужно Без ON получится лишнее перемножение rows. ### Где это встречается в backend JOIN нужен, чтобы API вернул данные ресурса вместе со связанными сущностями. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: self join». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: self join» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую
Step mix: {'theory': 2, 'test': 2, 'practice': 3, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 3, 'ide_plugin': 2}

## 011. SQL: подзапросы

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это SELECT читает columns из table и возвращает rows. ### Зачем это нужно Не используй SELECT * без причины. ### Где это встречается в backend Любой endpoint чтения начинается с SELECT. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: подзапросы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: подзапросы» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от следующего
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 012. SQL: CTE

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это CTE через WITH даёт имя промежуточному query. ### Зачем это нужно CTE действует только внутри query. ### Где это встречается в backend CTE делает сложные reports читаемыми. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: CTE». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: CTE» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от следующего шага: здесь важны поня
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 013. SQL: оконные функции

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это Оконные функции считают значение по окну строк и не уменьшают количество строк в результате. Ключевые слова: `OVER`, `PARTITION BY`, `ORDER BY`. ### Зачем это нужно В отличие от `GROUP BY`, окно не схлопывает строки. Каждая строка заказа остаётся в результате, но получает дополнительное вычисленное значение. ### Где это встречается в backend Window functions используют для ranking, row_number, running total и выбора последней записи по группе: например, последний заказ каждого пользователя. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: оконные функции
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 014. SQL: INSERT, UPDATE, DELETE

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это INSERT, UPDATE и DELETE меняют rows в database. ### Зачем это нужно Проверяй WHERE перед UPDATE/DELETE. ### Где это встречается в backend CRUD endpoints используют эти команды для ресурсов. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: INSERT, UPDATE, DELETE». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: INSERT, UPDATE, DELETE» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 015. SQL: constraints

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это Constraints защищают данные: PRIMARY KEY, NOT NULL, UNIQUE, DEFAULT, FOREIGN KEY. ### Зачем это нужно Ключевые правила держи в database. ### Где это встречается в backend Database не должна принимать невозможное состояние даже при bug в API. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: constraints». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: constraints» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 016. SQL: нормализация

Estimated hours: 3.9
Topics: SQL

Goal: ### Что это SELECT читает columns из table и возвращает rows. ### Зачем это нужно Не используй SELECT * без причины. ### Где это встречается в backend Любой endpoint чтения начинается с SELECT. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: нормализация». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: нормализация» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отличается от следую
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 017. SQL: transactions и ACID

Estimated hours: 3.9
Topics: SQL, транзакции

Goal: ### Что это Transaction объединяет изменения: COMMIT сохраняет, ROLLBACK откатывает. ### Зачем это нужно ACID означает «всё или ничего» для атомарности. ### Где это встречается в backend Оплата и перевод денег должны быть атомарными. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: transactions и ACID». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: transactions и ACID» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убр
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 018. SQL: изоляция, блокировки, индексы

Estimated hours: 3.9
Topics: SQL, уровни изоляции, блокировки, индексы

Goal: ### Что это SELECT читает columns из table и возвращает rows. ### Зачем это нужно Не используй SELECT * без причины. ### Где это встречается в backend Любой endpoint чтения начинается с SELECT. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQL: изоляция, блокировки, индексы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQL: изоляция, блокировки, индексы» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку.
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 019. SQLite: файл базы

Estimated hours: 3.9
Topics: SQLite/sqlite3, SQL

Goal: ### Что это SQLite хранит базу в `.db file` и не требует отдельного сервера. ### Зачем это нужно Для foreign keys в SQLite нужна PRAGMA. ### Где это встречается в backend Подходит для локальных утилит, прототипов и temporary database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLite: файл базы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLite: файл базы» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 020. SQLite: sqlite3 CLI

Estimated hours: 3.9
Topics: SQLite/sqlite3, SQL

Goal: ### Что это SQLite хранит базу в `.db file` и не требует отдельного сервера. ### Зачем это нужно Для foreign keys в SQLite нужна PRAGMA. ### Где это встречается в backend Подходит для локальных утилит, прототипов и temporary database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLite: sqlite3 CLI». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLite: sqlite3 CLI» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключе
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 021. SQLite: DDL и constraints

Estimated hours: 3.9
Topics: SQLite/sqlite3, SQL

Goal: ### Что это SQLite хранит базу в `.db file` и не требует отдельного сервера. ### Зачем это нужно Для foreign keys в SQLite нужна PRAGMA. ### Где это встречается в backend Подходит для локальных утилит, прототипов и temporary database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLite: DDL и constraints». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLite: DDL и constraints» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 022. SQLite: CRUD

Estimated hours: 3.9
Topics: SQLite/sqlite3, SQL

Goal: ### Что это SQLite хранит базу в `.db file` и не требует отдельного сервера. ### Зачем это нужно Для foreign keys в SQLite нужна PRAGMA. ### Где это встречается в backend Подходит для локальных утилит, прототипов и temporary database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLite: CRUD». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLite: CRUD» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Та
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 023. SQLite: transactions и PRAGMA

Estimated hours: 3.9
Topics: SQLite/sqlite3, SQL, транзакции

Goal: ### Что это SQLite хранит базу в `.db file` и не требует отдельного сервера. ### Зачем это нужно Для foreign keys в SQLite нужна PRAGMA. ### Где это встречается в backend Подходит для локальных утилит, прототипов и temporary database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLite: transactions и PRAGMA». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLite: transactions и PRAGMA» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появитс
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 024. sqlite3 через Python

Estimated hours: 3.9
Topics: SQLite/sqlite3, SQL, pytest

Goal: ### Что это sqlite3 открывает `.db file`, выполняет SQL через execute и сохраняет изменения через commit. ### Зачем это нужно Данные передавай параметрами, а не склейкой строк. ### Где это встречается в backend Это основа repository layer и temporary database в проверках. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «sqlite3 через Python». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «sqlite3 через Python» проверь три вещи: какой вход рассматривается, какой результат получается и
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 025. SQLite repository и проекты

Estimated hours: 3.9
Topics: SQLite/sqlite3, SQL, pytest

Goal: ### Что это SQLite хранит базу в `.db file` и не требует отдельного сервера. ### Зачем это нужно Для foreign keys в SQLite нужна PRAGMA. ### Где это встречается в backend Подходит для локальных утилит, прототипов и temporary database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLite repository и проекты». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLite repository и проекты» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, е
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 026. NoSQL: документы

Estimated hours: 3.9
Topics: NoSQL

Goal: ### Что это NoSQL: документы объясняет конкретный способ хранения, передачи или выборки данных. ### Зачем это нужно Порядок и фильтр должны быть явными. ### Где это встречается в backend В backend эта тема влияет на API, database и надёжность сервиса. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «NoSQL: документы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «NoSQL: документы» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если у
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 027. Redis

Estimated hours: 4.2
Topics: Redis

Goal: ### Что это Redis — key/value хранилище в памяти для cache, TTL, counter, rate limit и lock. ### Зачем это нужно После изменения данных нужна invalidation старого key. ### Где это встречается в backend API сначала делает GET по key, при промахе читает базу и делает SET с expire. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Redis». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Redis» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится,
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 028. ClickHouse и OLAP

Estimated hours: 3.9
Topics: ClickHouse / OLAP, SQL

Goal: ### Что это ClickHouse — колоночная OLAP database для больших аналитических выборок. ### Зачем это нужно OLAP не заменяет transactional database. ### Где это встречается в backend Используется для events, payments, reports и продуктовой аналитики. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «ClickHouse и OLAP». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «ClickHouse и OLAP» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убр
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 029. S3 и MinIO

Estimated hours: 4.2
Topics: S3 / MinIO

Goal: ### Что это S3 хранит objects внутри bucket. Object key — строковое имя объекта; MinIO даёт S3-compatible сервис локально. ### Зачем это нужно Object key похож на path, но это имя объекта. ### Где это встречается в backend Так хранят avatars, documents, exports и backups вне relational database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «S3 и MinIO». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «S3 и MinIO» проверь три вещи: какой вход рассматривается, какой результат получаетс
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 030. Сети: TCP и UDP

Estimated hours: 4.2
Topics: сети, TCP / UDP

Goal: ### Что это Сети: TCP и UDP объясняет конкретный способ хранения, передачи или выборки данных. ### Зачем это нужно Порядок и фильтр должны быть явными. ### Где это встречается в backend В backend эта тема влияет на API, database и надёжность сервиса. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Сети: TCP и UDP». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Сети: TCP и UDP» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убра
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 031. HTTP и HTTPS

Estimated hours: 4.2
Topics: HTTP / HTTPS, сети

Goal: ### Что это HTTP — request/response: method, path, headers, body и status. ### Зачем это нужно Status должен отражать смысл ошибки или успеха. ### Где это встречается в backend Каждый API endpoint работает через HTTP; HTTPS добавляет TLS. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «HTTP и HTTPS». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «HTTP и HTTPS» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 032. REST и авторизация

Estimated hours: 4.9
Topics: REST, авторизация, HTTP / HTTPS, FastAPI

Goal: ### Что это REST и авторизация объясняет конкретный способ хранения, передачи или выборки данных. ### Зачем это нужно Порядок и фильтр должны быть явными. ### Где это встречается в backend В backend эта тема влияет на API, database и надёжность сервиса. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «REST и авторизация». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «REST и авторизация» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится,
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 033. SOAP, GraphQL, gRPC, WebSockets

Estimated hours: 4.2
Topics: SOAP / GraphQL / gRPC / WebSockets обзорно, HTTP / HTTPS

Goal: ### Что это SOAP, GraphQL, gRPC, WebSockets объясняет конкретный способ хранения, передачи или выборки данных. ### Зачем это нужно Порядок и фильтр должны быть явными. ### Где это встречается в backend В backend эта тема влияет на API, database и надёжность сервиса. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SOAP, GraphQL, gRPC, WebSockets». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SOAP, GraphQL, gRPC, WebSockets» проверь три вещи: какой вход рассматривается, какой результ
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 6, 'ide_plugin': 2}

## 034. Проектирование схемы

Estimated hours: 3.9
Topics: SQL, индексы

Goal: ### Что это Проектирование схемы объясняет конкретный способ хранения, передачи или выборки данных. ### Зачем это нужно Порядок и фильтр должны быть явными. ### Где это встречается в backend В backend эта тема влияет на API, database и надёжность сервиса. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Проектирование схемы». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Проектирование схемы» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появ
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 035. Индексы и EXPLAIN

Estimated hours: 3.9
Topics: SQL, индексы

Goal: ### Что это Index ускоряет поиск, а EXPLAIN показывает query plan. ### Зачем это нужно Индекс должен поддерживать конкретный частый query. ### Где это встречается в backend Индексы нужны для частых WHERE, JOIN и ORDER BY в API. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Индексы и EXPLAIN». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Индексы и EXPLAIN» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку.
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 036. Экзамен 3

Estimated hours: 3.9
Topics: SQL, SQLite/sqlite3, сети

Goal: ### Что это Экзамен собирает уже изученные навыки в одну задачу с понятным контрактом. ### Зачем это нужно Перед кодом выпиши вход, выход и ошибки. ### Где это встречается в backend В backend так выглядят реальные задачи: прочитать требования, выделить edge cases, написать код и проверить. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Экзамен 3». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Экзамен 3» проверь три вещи: какой вход рассматривается, какой результат получается и кака
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

# Модуль 4. FastAPI, инфраструктура и тестирование

## 001. FastAPI: первый API

Estimated hours: 4.9
Topics: FastAPI, HTTP / HTTPS, REST

Goal: ### Что это API — это способ, которым одна программа обращается к другой. Браузер, мобильное приложение или другой backend отправляет HTTP-запрос, а сервер возвращает HTTP-ответ. В первом FastAPI-уроке не нужны база, авторизация и сложные данные. Здесь важно связать самый маленький путь с Python-функцией. ### Минимальный пример ### Разбор по строкам `FastAPI()` создаёт приложение. `@app.get("/health")` говорит: если пришёл GET-запрос на путь `/health`, вызови функцию ниже. `return {"status": "ok"}` превращается в JSON-ответ. ### Что важно FastAPI связывает HTTP-метод, путь и Python-функцию. Если метод или путь отличаются от условия, клиент по
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 002. FastAPI: GET route

Estimated hours: 4.9
Topics: FastAPI, REST

Goal: ### Что это FastAPI связывает route, параметры, Pydantic-модели, response_model и HTTP status. Контекст этого урока: «FastAPI: GET route». ### Зачем это нужно Method и path в коде должны совпадать с условием задачи. ### Где это встречается в backend На этих контрактах строится API: health, tasks, users и errors. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «FastAPI: GET route». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «FastAPI: GET route» проверь три вещи: какой вход рассматри
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 003. FastAPI: path params

Estimated hours: 4.9
Topics: FastAPI, REST

Goal: ### Что это FastAPI связывает route, параметры, Pydantic-модели, response_model и HTTP status. Контекст этого урока: «FastAPI: path params». ### Зачем это нужно Method и path в коде должны совпадать с условием задачи. ### Где это встречается в backend На этих контрактах строится API: health, tasks, users и errors. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «FastAPI: path params». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «FastAPI: path params» проверь три вещи: какой вход рас
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 004. FastAPI: query params

Estimated hours: 4.9
Topics: FastAPI, REST

Goal: ### Что это FastAPI связывает route, параметры, Pydantic-модели, response_model и HTTP status. Контекст этого урока: «FastAPI: query params». ### Зачем это нужно Method и path в коде должны совпадать с условием задачи. ### Где это встречается в backend На этих контрактах строится API: health, tasks, users и errors. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «FastAPI: query params». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «FastAPI: query params» проверь три вещи: какой вход
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 005. FastAPI: request body

Estimated hours: 4.9
Topics: FastAPI, REST

Goal: ### Что это FastAPI связывает route, параметры, Pydantic-модели, response_model и HTTP status. Контекст этого урока: «FastAPI: request body». ### Зачем это нужно Method и path в коде должны совпадать с условием задачи. ### Где это встречается в backend На этих контрактах строится API: health, tasks, users и errors. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «FastAPI: request body». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «FastAPI: request body» проверь три вещи: какой вход
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 006. Pydantic models

Estimated hours: 4.9
Topics: FastAPI

Goal: ### Что это Pydantic-модель описывает структуру данных: какие поля есть, какие типы ожидаются, какие ограничения действуют. Это снижает количество ручных проверок. ### Минимальный пример ### Разбор по строкам `title` должен быть строкой минимум из одного символа. Если клиент отправит пустую строку, FastAPI вернёт 422. ### Что важно FastAPI связывает HTTP-метод, путь и Python-функцию. Если метод или путь отличаются от условия, клиент попадёт не туда или получит ошибку. Контекст этого урока: «Pydantic models». ### Частая ошибка Не оставляй важные ограничения только в тексте условия. Запиши их в модель. ### Самопроверка Где лучше проверять, что
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 007. response_model и status codes

Estimated hours: 4.9
Topics: FastAPI, HTTP / HTTPS

Goal: ### Что это `response_model` описывает форму ответа. Это полезно, когда внутренний объект содержит лишние поля, а клиент должен увидеть только безопасный контракт. ### Минимальный пример ### Разбор по строкам FastAPI отфильтрует ответ по TaskOut. Клиент не увидит `secret`. ### Что важно FastAPI связывает HTTP-метод, путь и Python-функцию. Если метод или путь отличаются от условия, клиент попадёт не туда или получит ошибку. Контекст этого урока: «response_model и status codes». ### Частая ошибка Не возвращай внутренние поля наружу случайно. Контракт ответа должен быть явным. ### Самопроверка Зачем нужен response_model?
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 008. HTTPException

Estimated hours: 4.9
Topics: FastAPI, HTTP / HTTPS

Goal: ### Что это HTTPException объясняет конкретный способ хранения, передачи или выборки данных. ### Зачем это нужно Порядок и фильтр должны быть явными. ### Где это встречается в backend В backend эта тема влияет на API, database и надёжность сервиса. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «HTTPException». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «HTTPException» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать клю
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 009. Routers

Estimated hours: 4.0
Topics: FastAPI

Goal: ### Что это Тема «Routers» вводит отдельный навык, который дальше используется в задачах курса. ### Зачем это нужно Отделяй новое понятие от имён переменных: имя можно заменить, а смысл операции должен остаться тем же. Контекст этого урока: «Routers». ### Где это встречается в backend В backend-разработке «Routers» используется там, где нужно получить устойчивое поведение кода и понятный результат. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Routers». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика по
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 010. Depends и settings

Estimated hours: 4.0
Topics: FastAPI

Goal: ### Dependency `Depends` показывает, что endpoint получает значение не из URL и не из body, а из отдельной функции-зависимости. Так endpoint остаётся коротким: он просит готовые settings, текущий лимит или сервис, а сбор деталей живёт отдельно. Минимальный пример: `get_settings` собирает конфиг. `Depends(get_settings)` связывает endpoint с зависимостью. В настоящем FastAPI зависимость вызовет framework. Где это встречается в backend: settings, database session, current user, feature flags и service objects. Частая ошибка: читать env или создавать соединение прямо в каждом endpoint. Самопроверка: какая функция является зависимостью?
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 011. Middleware и logging

Estimated hours: 4.0
Topics: FastAPI

Goal: ### Что это Тема «Middleware и logging» вводит отдельный навык, который дальше используется в задачах курса. ### Зачем это нужно Отделяй новое понятие от имён переменных: имя можно заменить, а смысл операции должен остаться тем же. Контекст этого урока: «Middleware и logging». ### Где это встречается в backend В backend-разработке «Middleware и logging» используется там, где нужно получить устойчивое поведение кода и понятный результат. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Middleware и logging». Сначала найди термин урока, затем строку, где он влияет на данны
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 012. Healthcheck

Estimated hours: 4.0
Topics: FastAPI, Docker

Goal: ### Что это Тема «Healthcheck» вводит отдельный навык, который дальше используется в задачах курса. ### Зачем это нужно Отделяй новое понятие от имён переменных: имя можно заменить, а смысл операции должен остаться тем же. Контекст этого урока: «Healthcheck». ### Где это встречается в backend В backend-разработке «Healthcheck» используется там, где нужно получить устойчивое поведение кода и понятный результат. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Healthcheck». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ##
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 013. Auth JWT

Estimated hours: 4.9
Topics: FastAPI, авторизация

Goal: ### Что это FastAPI связывает route, параметры, Pydantic-модели, response_model и HTTP status. Контекст этого урока: «Auth JWT». ### Зачем это нужно Method и path в коде должны совпадать с условием задачи. ### Где это встречается в backend На этих контрактах строится API: health, tasks, users и errors. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Auth JWT». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Auth JWT» проверь три вещи: какой вход рассматривается, какой результат получа
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 014. Refresh tokens

Estimated hours: 4.9
Topics: FastAPI, авторизация

Goal: ### Что это Refresh tokens уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Refresh tokens». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Refresh tokens» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Тако
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 015. Database session

Estimated hours: 3.9
Topics: FastAPI, SQLAlchemy

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Database session». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Database session» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 016. SQLAlchemy engine/session

Estimated hours: 3.9
Topics: SQLAlchemy, FastAPI

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLAlchemy engine/session». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLAlchemy engine/session» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошиб
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 017. SQLAlchemy models

Estimated hours: 3.9
Topics: SQLAlchemy, FastAPI

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLAlchemy models». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLAlchemy models» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, есл
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 018. SQLAlchemy relationships

Estimated hours: 3.9
Topics: SQLAlchemy, FastAPI

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLAlchemy relationships». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLAlchemy relationships» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 019. SQLAlchemy transactions

Estimated hours: 3.9
Topics: SQLAlchemy, транзакции, FastAPI

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «SQLAlchemy transactions». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «SQLAlchemy transactions» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка п
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 020. Repository и unit of work

Estimated hours: 3.9
Topics: SQLAlchemy, FastAPI

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Repository и unit of work». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Repository и unit of work» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошиб
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 021. Alembic migrations

Estimated hours: 3.9
Topics: SQLAlchemy, FastAPI

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Alembic migrations». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Alembic migrations» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, е
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 022. Pagination, filtering, sorting

Estimated hours: 4.9
Topics: FastAPI

Goal: ### Что это Pagination, filtering, sorting уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Pagination, filtering, sorting». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Pagination, filtering, sorting» проверь три вещи: какой вход рассматривается, какой результат получается и какая ош
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 023. TestClient

Estimated hours: 4.9
Topics: FastAPI, pytest

Goal: ### Что это TestClient уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «TestClient». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «TestClient» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор отл
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 024. Integration tests

Estimated hours: 4.9
Topics: FastAPI, pytest

Goal: ### Что это Интеграционная проверка запускает несколько слоёв вместе: HTTP route, validation, service, database session и JSON-ответ. Она нужна, чтобы поймать ошибку на стыке компонентов, которую не видно в маленькой функции. ### Зачем это нужно База для такой проверки должна быть изолированной: данные одного запуска не должны влиять на другой запуск. ### Где это встречается в backend В backend такие проверки проходят перед релизом: создать ресурс через API, прочитать его обратно, проверить status code и убедиться, что данные прошли через приложение. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 025. OpenAPI docs

Estimated hours: 4.9
Topics: FastAPI

Goal: ### Что это OpenAPI docs уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «OpenAPI docs». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «OpenAPI docs» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разб
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 026. Postman collections

Estimated hours: 4.9
Topics: Postman, FastAPI

Goal: ### Что это Postman collections уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Postman collections». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Postman collections» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключев
Step mix: {'theory': 2, 'test': 2, 'practice': 6, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'http_api': 6, 'ide_plugin': 2}

## 027. CRUD-проект

Estimated hours: 3.9
Topics: CRUD-проект, FastAPI, SQLAlchemy

Goal: ### Что это CRUD-проект уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «CRUD-проект». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «CRUD-проект» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать ключевую строку. Такой разбор
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 028. Dockerfile и image

Estimated hours: 4.2
Topics: Docker

Goal: ### Что это Docker описывает и запускает приложение в container: image, layers, ports, env и volumes. Контекст этого урока: «Dockerfile и image». ### Зачем это нужно Копируй dependencies отдельно, чтобы Docker cache работал. ### Где это встречается в backend Так API одинаково работает локально, в CI и на сервере. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Dockerfile и image». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Dockerfile и image» проверь три вещи: какой вход рассматр
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 029. Container, ports, env

Estimated hours: 4.2
Topics: Docker

Goal: ### Что это Docker описывает и запускает приложение в container: image, layers, ports, env и volumes. Контекст этого урока: «Container, ports, env». ### Зачем это нужно Копируй dependencies отдельно, чтобы Docker cache работал. ### Где это встречается в backend Так API одинаково работает локально, в CI и на сервере. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Container, ports, env». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Container, ports, env» проверь три вещи: какой вход
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 030. Volumes и .dockerignore

Estimated hours: 4.2
Topics: Docker

Goal: ### Что это Docker описывает и запускает приложение в container: image, layers, ports, env и volumes. Контекст этого урока: «Volumes и .dockerignore». ### Зачем это нужно Копируй dependencies отдельно, чтобы Docker cache работал. ### Где это встречается в backend Так API одинаково работает локально, в CI и на сервере. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Volumes и .dockerignore». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Volumes и .dockerignore» проверь три вещи: како
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 031. multistage build

Estimated hours: 4.2
Topics: Docker, multistage build

Goal: ### Что это Docker описывает и запускает приложение в container: image, layers, ports, env и volumes. Контекст этого урока: «multistage build». ### Зачем это нужно Копируй dependencies отдельно, чтобы Docker cache работал. ### Где это встречается в backend Так API одинаково работает локально, в CI и на сервере. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «multistage build». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «multistage build» проверь три вещи: какой вход рассматриваетс
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 032. Docker Compose: app + db

Estimated hours: 4.2
Topics: Docker Compose, Docker

Goal: ### Что это Docker описывает и запускает приложение в container: image, layers, ports, env и volumes. Контекст этого урока: «Docker Compose: app + db». ### Зачем это нужно Копируй dependencies отдельно, чтобы Docker cache работал. ### Где это встречается в backend Так API одинаково работает локально, в CI и на сервере. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Docker Compose: app + db». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Docker Compose: app + db» проверь три вещи: к
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 033. Healthcheck, logs, exec, networks, secrets

Estimated hours: 4.2
Topics: Docker, Docker Compose

Goal: ### Что это Healthcheck, logs, exec, networks, secrets уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Healthcheck, logs, exec, networks, secrets». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Healthcheck, logs, exec, networks, secrets» проверь три вещи: какой вход рассматривается, к
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 034. CI/CD: GitHub Actions и GitLab CI

Estimated hours: 4.2
Topics: CI/CD, GitHub Actions, GitLab CI/CD

Goal: ### Что это Remote — удалённый repo. GitHub хранит ветки и запускает review/CI. ### Зачем это нужно Non-fast-forward означает, что remote ушёл вперёд. ### Где это встречается в backend Через remote команда делает pull request, CI и релизы. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «CI/CD: GitHub Actions и GitLab CI». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «CI/CD: GitHub Actions и GitLab CI» проверь три вещи: какой вход рассматривается, какой результат получается и какая о
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

# Модуль 5. AI-интеграция, деплой и финальный проект

## 001. AI API request

Estimated hours: 3.7
Topics: AI API

Goal: ### Что это AI API request — это обычный HTTP-like контракт: payload, messages, provider, timeout и проверяемый ответ. В проверках используется mock provider, чтобы результат не зависел от внешнего сервиса. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «AI API request». Где это встречается в backend: Так backend-сервис вызывает AI-провайдера безопасно и повторяемо. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «AI API request». Самопроверка:
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 002. Prompt и messages

Estimated hours: 3.7
Topics: AI API

Goal: ### Что это Prompt управляет тем, что модель должна сделать, а messages разделяют правила и запрос пользователя. В backend это помогает не смешивать системные правила, пользовательский ввод и проверяемый контекст. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Prompt и messages». Где это встречается в backend: Так строят безопасные AI-помощники, ревьюеры кода и RAG-ответы. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Prompt и messages». Сам
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 003. Structured output

Estimated hours: 3.4
Topics: AI API

Goal: ### Что это Structured output заставляет модель вернуть данные в ожидаемой форме, например dict с answer и confidence. Это нужно, чтобы backend не парсил свободный текст наугад. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Structured output». Где это встречается в backend: Так API может валидировать ответ модели перед записью в базу или показом пользователю. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Structured output». Самопроверка: ка
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 004. JSON schema

Estimated hours: 3.4
Topics: AI API

Goal: ### Что это JSON schema описывает обязательные поля и типы AI-ответа. Схема превращает свободный ответ в проверяемый контракт. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «JSON schema». Где это встречается в backend: Так сервис отклоняет ответ, если модель вернула неверную форму. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «JSON schema». Самопроверка: какая строка задаёт AI-специфичный контракт? Разбор примера для темы «JSON schema»: снач
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 005. Mock provider

Estimated hours: 3.7
Topics: AI API, pytest

Goal: ### Что это Mock provider имитирует внешний AI-сервис внутри тестов. Так можно проверить payload, ошибки и retries без платного API. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Mock provider». Где это встречается в backend: Mock используют в unit-тестах сервисного слоя и CI. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Mock provider». Самопроверка: какая строка задаёт AI-специфичный контракт? Разбор примера для темы «Mock provider»: снач
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 006. Retries и timeouts

Estimated hours: 3.7
Topics: AI API

Goal: ### Что это Timeout защищает сервис от зависания, retry даёт шанс пережить временную ошибку. Эти правила нужны для внешних API, которые иногда отвечают медленно или падают. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Retries и timeouts». Где это встречается в backend: Так backend не держит запрос пользователя бесконечно. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Retries и timeouts». Самопроверка: какая строка задаёт AI-специфичный кон
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 007. Rate limits и cost

Estimated hours: 3.7
Topics: AI API

Goal: ### Что это Rate limit ограничивает частоту запросов, cost guard ограничивает цену. AI API может быть дорогим, поэтому backend считает tokens и откладывает лишние запросы. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Rate limits и cost». Где это встречается в backend: Так сервис остаётся предсказуемым при большом потоке студентов. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Rate limits и cost». Самопроверка: какая строка задаёт AI-специф
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 008. RAG chunks

Estimated hours: 3.4
Topics: RAG

Goal: ### Что это RAG сначала режет документы на chunks, затем ищет подходящий источник и отвечает только с опорой на него. Это снижает риск выдуманного ответа. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «RAG chunks». Где это встречается в backend: Так помощник курса отвечает по материалам, а не по памяти модели. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «RAG chunks». Самопроверка: какая строка задаёт AI-специфичный контракт? Разбор примера
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 009. Embeddings и vector DB

Estimated hours: 3.7
Topics: vector databases, RAG

Goal: ### Что это Embedding превращает текст в вектор, чтобы искать похожие фрагменты. В курсе используется mock embedding, чтобы проверить top-k и metadata без внешней базы. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Embeddings и vector DB». Где это встречается в backend: Так строится retrieval-слой перед RAG-ответом. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Embeddings и vector DB». Самопроверка: какая строка задаёт AI-специфичный контра
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 010. Retrieval evaluation hallucination

Estimated hours: 3.7
Topics: RAG, vector databases

Goal: ### Что это Evaluation проверяет, есть ли у ответа источники, релевантность retrieval и неподтверждённые claims. Без оценки AI-помощник может уверенно показать выдумку. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Retrieval evaluation hallucination». Где это встречается в backend: Так команда контролирует качество AI-ответов перед запуском. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Retrieval evaluation hallucination». Самопроверка: как
Step mix: {'theory': 2, 'test': 2, 'practice': 5, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 5, 'ide_plugin': 2}

## 011. LangChain и LangGraph

Estimated hours: 3.4
Topics: LangChain / LangGraph концептуально

Goal: ### Что это Граф AI-пайплайна хранит state и переводит его между node: retrieve, answer, retry, finish. Так сложный AI-flow становится явным и тестируемым. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «LangChain и LangGraph». Где это встречается в backend: Так в backend строят управляемые цепочки действий. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «LangChain и LangGraph». Самопроверка: какая строка задаёт AI-специфичный контракт? Разбор
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 012. Безопасный вайбкодинг

Estimated hours: 3.4
Topics: вайбкодинг

Goal: ### Что это Безопасный вайбкодинг означает: AI может предложить diff, но человек проверяет секреты, зависимости, API и тесты. Нельзя принимать код только потому, что он выглядит убедительно. Минимальный пример: Разбери пример: вход превращается в явный payload или набор chunks, затем код получает проверяемый ответ без платного API. Контекст этого урока: «Безопасный вайбкодинг». Где это встречается в backend: Так команда использует AI как помощника, а не как источник непроверенных изменений. Частая ошибка: заменить AI-задачу обычной строковой функцией без messages, provider, schema, retrieval или review-контракта. Урок: «Безопасный вайбкодинг»
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'python_pytest': 4, 'ide_plugin': 2}

## 013. VPS, безопасность, домен, SSL

Estimated hours: 4.2
Topics: VPS, безопасность сервера, домен, SSL

Goal: ### Что это Deploy переносит release на сервер, проверяет healthcheck и оставляет rollback path. ### Зачем это нужно Новый release нельзя считать успешным без healthcheck. ### Где это встречается в backend Production backend держится на домене, SSL, env, reverse proxy и повторяемом release. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «VPS, безопасность, домен, SSL». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «VPS, безопасность, домен, SSL» проверь три вещи: какой вход рассматри
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 014. Автодеплой и rollback

Estimated hours: 4.2
Topics: автодеплой, CI/CD

Goal: ### Что это Deploy переносит release на сервер, проверяет healthcheck и оставляет rollback path. ### Зачем это нужно Новый release нельзя считать успешным без healthcheck. ### Где это встречается в backend Production backend держится на домене, SSL, env, reverse proxy и повторяемом release. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Автодеплой и rollback». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Автодеплой и rollback» проверь три вещи: какой вход рассматривается, какой ре
Step mix: {'theory': 2, 'test': 1, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 1, 'ide_plugin': 6}

## 015. Финальный проект: ТЗ

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: ТЗ фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: ТЗ». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: ТЗ». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: ТЗ». Сначала найди термин урока, затем строку, гд
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 016. Финальный проект: User stories

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: User stories фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: User stories». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: User stories». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: User stories». Снач
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 017. Финальный проект: схема БД

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: схема БД фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: схема БД». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: схема БД». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: схема БД». Сначала найди термин
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 018. Финальный проект: API contract

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: API contract фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: API contract». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: API contract». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: API contract». Снач
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 019. Финальный проект: Git workflow

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest, Git и GitHub

Goal: ### Что это Remote — удалённый repo. GitHub хранит ветки и запускает review/CI. ### Зачем это нужно Non-fast-forward означает, что remote ушёл вперёд. ### Где это встречается в backend Через remote команда делает pull request, CI и релизы. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: Git workflow». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: Git workflow» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 020. Финальный проект: FastAPI-структура

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это FastAPI связывает route, параметры, Pydantic-модели, response_model и HTTP status. Контекст этого урока: «Финальный проект: FastAPI-структура». ### Зачем это нужно Method и path в коде должны совпадать с условием задачи. ### Где это встречается в backend На этих контрактах строится API: health, tasks, users и errors. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: FastAPI-структура». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: FastAP
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 021. Финальный проект: SQLAlchemy models

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: SQLAlchemy models». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: SQLAlchemy models» проверь три вещи: какой вход рассматривается, какой результат пол
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 022. Финальный проект: Alembic migrations

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это SQLAlchemy отделяет Python-objects от SQL, а session управляет transaction. ### Зачем это нужно Engine создаётся один раз, session — на request. ### Где это встречается в backend ORM models, repository и migrations связывают API с database. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: Alembic migrations». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: Alembic migrations» проверь три вещи: какой вход рассматривается, какой результат п
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 023. Финальный проект: Auth

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это FastAPI связывает route, параметры, Pydantic-модели, response_model и HTTP status. Контекст этого урока: «Финальный проект: Auth». ### Зачем это нужно Method и path в коде должны совпадать с условием задачи. ### Где это встречается в backend На этих контрактах строится API: health, tasks, users и errors. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: Auth». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: Auth» проверь три вещи: какой вх
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 024. Финальный проект: CRUD

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: CRUD фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: CRUD». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: CRUD». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: CRUD». Сначала найди термин урока, затем ст
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 025. Финальный проект: Tests

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Интеграционная проверка запускает несколько слоёв вместе: HTTP route, validation, service, database session и JSON-ответ. Она нужна, чтобы поймать ошибку на стыке компонентов, которую не видно в маленькой функции. ### Зачем это нужно База для такой проверки должна быть изолированной: данные одного запуска не должны влиять на другой запуск. ### Где это встречается в backend В backend такие проверки проходят перед релизом: создать ресурс через API, прочитать его обратно, проверить status code и убедиться, что данные прошли через приложение. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 026. Финальный проект: Docker

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest, Docker

Goal: ### Что это Docker описывает и запускает приложение в container: image, layers, ports, env и volumes. Контекст этого урока: «Финальный проект: Docker». ### Зачем это нужно Копируй dependencies отдельно, чтобы Docker cache работал. ### Где это встречается в backend Так API одинаково работает локально, в CI и на сервере. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: Docker». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: Docker» проверь три вещи: к
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 027. Финальный проект: CI/CD

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest, CI/CD, GitHub Actions, GitLab CI/CD

Goal: ### Что это CI/CD workflow запускает jobs: install, pytest, docker build и deploy. ### Зачем это нужно Разделяй test, build и deploy. ### Где это встречается в backend Pipeline не даёт сломанному коду попасть в main и production. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: CI/CD». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: CI/CD» проверь три вещи: какой вход рассматривается, какой результат получается и какая ошибка появится, если убрать кл
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 028. Финальный проект: AI integration

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest, AI API

Goal: ### Что это Финальный проект: AI integration уточняет контракт API и способ его проверки. ### Зачем это нужно Проверяй success и error path. ### Где это встречается в backend В backend это влияет на status codes, JSON, документацию и диагностику. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: AI integration». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: AI integration» проверь три вещи: какой вход рассматривается, какой результат получается и ка
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 029. Финальный проект: Logging

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: Logging фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: Logging». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: Logging». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: Logging». Сначала найди термин уро
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 030. Финальный проект: README

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: README фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: README». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: README». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: README». Сначала найди термин урока,
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 031. Финальный проект: Deploy

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest, VPS, автодеплой, домен, SSL

Goal: ### Что это Deploy переносит release на сервер, проверяет healthcheck и оставляет rollback path. ### Зачем это нужно Новый release нельзя считать успешным без healthcheck. ### Где это встречается в backend Production backend держится на домене, SSL, env, reverse proxy и повторяемом release. ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: Deploy». Сначала найди термин урока, затем строку, где он влияет на данные, состояние, запрос или команду. ### Диагностика понимания Для урока «Финальный проект: Deploy» проверь три вещи: какой вход рассматривается, ка
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}

## 032. Финальный проект: Final defense

Estimated hours: 3.9
Topics: финальный проект, FastAPI, SQLAlchemy, pytest

Goal: ### Что это Финальный проект: Final defense фиксирует отдельный gate финального проекта: требования, артефакт, проверку и pass/fail criteria. ### Зачем это нужно Gate должен проверять конкретный artifact: API contract, DB schema, auth, Docker, CI/CD или README. Контекст этого урока: «Финальный проект: Final defense». ### Где это встречается в backend Так финальный проект превращается в управляемую backend-систему, а не набор файлов. Контекст этого урока: «Финальный проект: Final defense». ### Концептуальный фрагмент ### Как читать фрагмент Этот фрагмент нужен не для копирования, а для понимания границы темы «Финальный проект: Final defense».
Step mix: {'theory': 2, 'test': 2, 'practice': 4, 'project': 2, 'summary': 1}
Checkers: {'quiz_single': 2, 'sql_query': 4, 'ide_plugin': 2}
