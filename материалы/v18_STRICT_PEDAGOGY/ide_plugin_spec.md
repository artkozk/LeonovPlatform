# IDE plugin spec v18

Плагин проверяет required_files, команды, чистое git-состояние, pytest, SQL, FastAPI, Docker/Compose, CI/CD, deploy dry-run и rollback dry-run. Для project-step проверка должна быть воспроизводимой без ручных действий.
